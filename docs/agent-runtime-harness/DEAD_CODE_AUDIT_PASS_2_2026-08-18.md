# Dead-code + readability audit, pass 2 — hermes plan (2026-08-18)

> **Home.** `docs/agent-runtime-harness/`, beside `REFACTOR_DEBT_AUDIT_2026-08-17.md` (Plan RD) whose §1.7 "already dead / not dead" lists this document extends and, in two places, corrects. Launcher twin: `Launcher_Brain/20 — Active Initiatives/dead-code-audit-pass-2.md` (the launcher plan; cross-stack rows below name it).
>
> **Repos as read:** hermes `40b0f0c53a` (lanes audited at `f7475d31d1..508e19c315`; the verifier re-proved every row at `40b0f0c53a` — the two ML-16 charsheet rebases touched no rowed symbol; H-CLI B4's fixture-drift count is the only STALE detail), launcher `0f205e38f`. **This is a read-only audit and plan. No code was changed.** Nothing under `X:/Eternia/.hermes/` was written; no `hermes serve/gateway/dashboard` was spawned; the two Python imports the verifier ran (`_EVENT_CONTRACTS`, the launcher's `dump_hermes_cli_contract.py`) used a scratchpad `HERMES_HOME`.
>
> **Coordination.** The launcher ledger's LIVE STATE (`Launcher_Brain/20 — Active Initiatives/plan-eg-weakness-escalation-ledger.md`, origin/main `5431d14d0`) reads **BOTH LANES EMPTY** as of 2026-08-18 ~07:50 EDT and permits this audit to write under the worktree → rebase → ff-only rule. Rows that touched the audit's EXCLUSION LIST (`tests/conftest.py`, `tests/agent_runtime/conftest.py`, `test_no_midtest_monkeypatch_undo.py`, the byte-frozen `test_office_class_key_one_fence.py` sha256 `1b0b0e82…989bad`, `tests/hermes_cli/{conftest,test_dashboard_tui_backcompat,test_config_read_guard,test_commands,test_gui_command,test_profiles}.py`, `hermes_cli/profiles.py`, `agent_runtime/{core_cache,store,errors,persona_assignments,persona_chat_continuity}.py`, `MISSION_CONTROL_LEDGER_REFACTOR_PLAN_2026-08-17.md`, `tests/agent_runtime/fixtures/**` + both stream `MANIFEST.sha256`) stay tagged `EXCL` so a re-lit lane can be honoured. **No row touches the B1 fence.** Golden-fixture regeneration is a cross-stack landing (precedent hermes `604063d8d6` + launcher `641b9b86a`) — any row that would alter a golden is a STAGE-STOP and sits in §6, never in a mechanical stage.
>
> **Standing rules for Phase 2:** fork boundary (`agent/` fork-added subdirs, `agent_runtime/`, `hermes_cli/harness*`, and files absent from `upstream/main` — `git cat-file -e upstream/main:<path>`); upstream files are never edited (fork-added blocks inside them are report-only); `tests/hermes_cli` runs ONLY via the per-file runner `scripts/run_tests.sh`; the full `agent_runtime` checkpoint `python -m pytest tests/agent_runtime/ -q` (~13–15 min, baseline **5669 / 1 skip / 0 fail**) is run by the PARENT in a background shell with the tail redirected to a file — for anything under `agent_runtime/`; explicit-path commits; `harness_parts/*.py` are exec'd, not imported (text grep, whole-scope); every removal gets a row in `tests/agent_runtime/test_tombstone_registry.py` (forms `MODULE` / `ATTR` / `CLASS_ATTR` / `IMPORT` / `EVENT` / `CODE` / `PATH`; the registry currently rows NONE of the names below); watch every deletion go RED first (restore the symbol → registry row red → re-delete).

**Evidence tags:** `CONF` verifier-confirmed by its own grep/AST at `40b0f0c53a` · `EXCL` touches an exclusion-list file · `NEW` found in the verifier's deeper pass · `RULING` operator decision needed · `STAGE-STOP` alters a golden.

---

## 0. Verdict up front

- **Fan-out:** four opus lanes — H-ART-A (`agent_runtime/` transport/projection cluster: serve_*, stream, snapshot, read_model, state_patches, patch_coverage, office_*, events, observability, realm/sync, store, models…), H-ART-B (`agent_runtime/` persona/mission-chat/board/skills/mcp/config…), H-CLI (`hermes_cli/harness.py` + `harness_support.py` + the six exec'd `harness_parts/`), H-PERIPH (fork-owned files outside those roots: `tools/{agent_chat_dispatch,agent_chat_tool,board_tool,path_identity,tool_full_descriptions}.py`, fork-added `hermes_cli/*` and `scripts/*`, `agent/charsheet/`, `mobile_core/`, fork-added blocks in upstream files). Each ran an AST-stripped executable-reference index (the tombstone registry's own `_code_only` mechanism — a symbol's retirement comment cannot vouch for it) over all 4,277 hermes `.py` files plus the launcher for wire tokens.
- **Verification:** one Fable verifier re-proved **59 rows** (every HIGH/MED A-row + the load-bearing count claims): CONFIRMED 51 · REFUTED 2 (+4 sub-rows) · DOWNGRADED 2 · STALE 1 · **NEW 2** (+1 correction). The parent independently re-proved `risk_flags.py`, the `_add_stage42_global_args` `cursor`/`since` fold, `certification_ladder.py`, the four `_atomic_write_json`/`_write_json_atomic` definitions, and the NEW-1 island (all intra-island refs) by hand.
- **Census authorities re-derived, not grepped:** 58 registered event kinds (`_EVENT_CONTRACTS` = `test_s15.SURVIVING_EVENT_COUNT`) — every one has a production literal, **0 dead**; 8 `@method` RPC handlers — **7 of 8** launcher-consumed (`runtime.office.unsubscribe` has zero launcher callers — a launcher gap, not a hermes deletion; NEW-2); 4 stream frame types, 3 fold-capability tokens, 5 read-model tables all consumed; 166 argparse command paths / 124 harness leaves (H-CLI's 334-flag count did NOT reproduce — 350 by the verifier's method; no row depends on it); 38 launcher capability-registry verbs vs `capability_descriptors()` — clean.
- **Headline corrections to documents everyone reads:** `REFACTOR_DEBT_AUDIT_2026-08-17.md` §0 Correction 4 / §1.7 / §5 and `SCOUT_HERMES_SWALLOW_AUDIT_2026-08-17.md` still say `runtime.office.resolve_conflict` "never landed, exactly seven RPC methods" — it landed at `32a392364b` four hours after that doc's commit; the registry holds **eight**; the real CLI-only verb is `office.actor.restore`. Also H-CLI's original "26 unspendable exit codes" is **22**: the four `install_*` codes are produced by fork-owned `scripts/install-mission-control-hermes.ps1` (exit 7 = the table's value), which the launcher runs — the lane's grep roots omitted `scripts/`.
- **Size of the prize (hermes):** ~490 lines mechanically deletable in-scope + ~184 (NEW-1 island) + ~250 test lines with it + ~57 `EXCL`; four rulings; one STAGE-STOP. Smaller than the launcher's by an order of magnitude — the July removal waves already took the big islands; what is left is pass-through parameters, write-only fields, byte-identical helper triplets, never-raised exceptions, and vocabularies that enforce nothing.

## 1. Rows — dead / removable (grouped by stage)

Column key: **kind** island / folded / pass-through / test-only / fake-affordance / dup-authority / plain-unused · **proof** compressed (a) symbol grep excluding decl (b) string/wire grep both repos (c) test-only check (d) `git log -S` · **needs** tombstone form, doc/plan text, test edits, cross-stack.

### 1.1 `agent_runtime/` transport + projection (H-ART-A) — suites named per stage; full `agent_runtime` checkpoint at the end of any stage that touches this dir

| stage | symbol · file | kind | proof | needs | ~lines | conf |
|---|---|---|---|---|---|---|
| HA-1 | `build_snapshot(task_store=, run_store=, incident_store=)` → `_build_snapshot_uncoalesced` → `_build_snapshot_in_runtime_scope` · `snapshot.py` | pass-through / fake affordance (injecting a store only bypasses coalescing; the comment "Injected stores (tests, doctors) must observe…" claims the opposite) | AST `Name(Load)` walk: the three names are never loaded (`agent_store`/`event_log`/`prompt_skills_catalogs` are); `status.py::build_status` is a DIFFERENT function that does read them | drop 3 params + the comment (B3 becomes true); 2 test edits (`test_observability.py`, `test_snapshot_coalesce.py`); no tombstone (params); red-first: `build_snapshot(run_store=<sentinel>)` produces a frame identical to `build_snapshot()` today — that IS the bug | 12 | CONF HIGH |
| HA-1 | `_risk_if_ignored(kind, severity)` · `observability.py` | dropped parameter | AST: `kind` never loaded; 1 call site | drop param + call-site arg | 2 | CONF HIGH |
| HA-1 | `emit_persona_instance_remove(…, reason=)` · `state_patches.py` | dropped parameter (computed by its caller, discarded; `emit_state_patch` has no `reason`) | 3 refs (decl, import, 1 call from `persona_assignments.py`) — the paired domain event carries its own reason | delete kwarg + docstring clause NOW; the caller-side `reason=safe_reason` edit is `EXCL` (`persona_assignments.py`) — harmless until that stage | 3 | CONF, partial `EXCL` |
| HA-2 | `_atomic_write_json` (`serve_registry.py`, `chat_session_scope.py`) vs `_write_json_atomic` (`serve_socket.py`, `mission_chat_steer.py`) | dup-authority (word-order-inverted names hid it) | docstring-stripped `ast.unparse` bodies hash-identical (`72180d60`); the two other same-named helpers exist and differ (return `bool` / take `payload`) — check before folding | home ONE in `serde.py`/`paths.py`; delete the other; pick one name repo-wide (B5) | ~22 | CONF HIGH |
| HA-2 | `_rows` ×3 · `read_model.py`, `snapshot.py`, `stream.py` | dup-authority | body hash `fa69aa36` ×3; no 4th copy | home ONE in `serde.py`; delete two | ~20 | CONF HIGH |
| HA-2 | `_text` (`stream.py`) vs `_optional_text` (`read_model.py`) **+ `runtime_hud._text`** (NEW third member) | dup-authority | hash `24effe08` ×**3** | home ONE (`_optional_text` — the honest name, B6); delete two | ~9 | CONF HIGH |
| HA-2 | `_safe_path_token` (`paths.py`) vs `_safe_token` (`realm_sync.py`) **+ `skill_promotion._safe_token`** (NEW) | dup-authority | hash `adc40a03` ×**3**; `repo_context._safe_path_token` is a different 1-liner | home in `paths.py`; import in both; delete two | ~8 | CONF HIGH |
| HA-2 (½ `EXCL`) | `_safe_id` (`office_store.py`) vs `_safe_model_id` (`store.py` `EXCL`) **+ `board_store._safe_id`** (NEW) | dup-authority | hash `0fb3936c` ×**3** | dedupe `office_store`+`board_store` NOW; the `store.py` copy joins HA-5 | ~7+3 | CONF HIGH |
| HA-3 | `realm_sync._board_artifacts`, `._office_artifacts` | island / advertised seam | 1 def + 1 doc line each (`MISSION_CONTROL_LEDGER_REFACTOR_PLAN_2026-08-17.md` `EXCL` doc) | delete; re-point the two plan lines (doc edit `EXCL`) | ~12 | CONF HIGH |
| HA-3 | `PersonaInstance.child_events_offset` · `models.py` (the S70 prune's missed fifth field, same block, same safety argument) | plain-unused wire-shaped field | 1 hit (decl); goldens 0; launcher 0 | delete; **no contract bump** — not on the wire | 1 | CONF HIGH |
| HA-3 | `GoalRuntimeInstance.started_by`, `.lease_expires_at`; `AgentRun.iterations_used` · `models.py` | plain-unused | decl-only (+ `started_by="test"` at one test site; 0 `AgentRun(` constructions in prod) | delete; drop the test kwarg (`test_s53_lane_write_lane_removal.py`) | 3 | CONF HIGH |
| HA-3 | `archive_orphaned_surface(…, updated_by="operator")` · `office_store.py` | dropped parameter — no attribution on the highest-blast-radius verb (2026-08-15 mass-archive precedent) | AST: never loaded; sole caller `harness_parts/office.py` passes only `dry_run` | **prefer WIRING it** (B8) over deleting | 1 | CONF HIGH |
| HA-3 | `ProjectionAccountant.dropped_by_design` · `parity.py` | test-only | 3 hits (def + `test_parity.py` + `test_running_work.py`); `summary()` does not read it | delete + 2 asserts | 3 | CONF MED |
| HA-4 (docs) | `REFACTOR_DEBT_AUDIT_2026-08-17.md` §0 Correction 4 / §1.7 / §5; `SCOUT_HERMES_SWALLOW_AUDIT_2026-08-17.md` "resolve_conflict never landed / seven RPC methods"; H-ART-A's own "8 of 8 launcher-consumed" | stale doc rot in the documents every lane is told to trust | `grep -c '^@method('` = 8; `serve_rpc.py` has `runtime.office.resolve_conflict`; launcher `runtime.office.unsubscribe` lib = 0 | dated superseded notes; "7 of 8" (NEW-2); ONE doc commit batched with H-CLI B5 and H-PERIPH's doc-17 wording | ~10 | CONF |
| HA-4 (docs) | `observability.py` literal `{"model_invalid_output"}` vs `incidents.MODEL_INVALID_OUTPUT` | single-name authority | pin `test_s54_individual_dead_symbols.py` asserts the literal in `getsource` + ledger row `tests/source_grep_debt.txt` (and its header count) — flip both IN THE SAME COMMIT | import the constant + coupled test/ledger edits | 4 | CONF HIGH |
| HA-5 `EXCL` | `IncidentStore.list_open_with_closed_count` · `store.py` (pairs with HA-1's `incident_store` finding) ; `store._safe_model_id` (HA-2's third copy) ; + the H-CLI follow-ons if those verbs are cut: `RealmStore.bind_server` → test-only-alive, `WorkspaceStore.add_agent`/`remove_agent` → dead | test-only / dup / follow-on | 5 hits (def + test double + 3 asserts), 0 prod | ONE `store.py` stage carrying all four; suites `test_store.py`, `test_observability.py`; sequence AFTER H-CLI-3 | ~57 | CONF `EXCL` |

Kept with reason (do not re-derive): `ALL_SECRET_ASSIGNMENT_PATTERNS`, `owned_keys`, the cache-reset seams, `HISTORICAL_*` fold sets, `MODEL_INVALID_OUTPUT` (KEPT pin), `MAX_KEY_LENGTH` (KEPT pin), all 8 RPC handlers, all 58 event kinds. `_accessible_skills_context` and `available_skills_context` in `prompt_observability.py` are NOT duplicate authorities (per-persona grant vs annotated catalog; both keys launcher-read) — H-ART-A's §D suspicion is refuted. `_cmd_observe` passing `runs=[]`/`incidents=[]` literals (`runtime_commands.py`) is a wire that can only report a constant — report-only, S47 item-5 class, cross-lane.

### 1.2 `agent_runtime/` persona · mission-chat · board · skills · mcp · config (H-ART-B)

| stage | symbol · file | kind | proof | needs | ~lines | conf |
|---|---|---|---|---|---|---|
| HB-1 | `agent_runtime/risk_flags.py` — whole module (`KNOWN_RISK_FLAGS` incl. the burn-in lane's `bounded_complex_burn_in`/`routing_burn_in_only`, `normalize_task_risk_flags`) + the `serde._coerce` arm `if annotation.__name__ == "Task"` | island behind a folded predicate — `agent_runtime` has had no `Task` class since the mission-lane removal (`test_models_serde.py` asserts `not hasattr(runtime_models, "Task")`); the only `class Task` in the tree is upstream `hermes_cli/kanban_db.py`, which never touches `serde` | prod refs: `serde.py` arm only; launcher 0; test readers `test_models_serde.py`, `test_persona_chat_history_curation.py` (uses `"risk_flags"` as a payload KEY — unrelated, stays green) | delete file + the 6-line arm; tombstone rows `MODULE agent_runtime.risk_flags` + `CODE normalize_task_risk_flags`; suites `scripts/run_tests.sh tests/agent_runtime/test_models_serde.py tests/agent_runtime/test_tombstone_registry.py tests/agent_runtime/test_persona_chat_history_curation.py`; `serde.py` is H-ART-A's file — HB-1 owns the whole cut | ~97 | CONF HIGH |
| HB-2 | `PLAN_ACTIONS` (`skill_promotion.py`; the comment claims it prevents the re-derivation the same file does four times), `PROMOTION_BLOCK_REASONS`, `WIRE_USER_MESSAGE_UNAVAILABLE_REASONS`, `DISPATCH_SESSION_REASONS` + the phantom `refuse_ambiguous_source` action nothing constructs (B4) | dup-authority / vacuous vocabulary registries (0 executable refs each) | `-w` 3/3/2/3 = decl + prose (`DISPATCH_SESSION_REASONS` + one test read) | delete all four + 2 test lines + 3 docstring sentences + 1 design-doc line; suites `test_dispatch_session_policy.py`, `test_skill_promotion.py`, `test_skill_publishability.py`, `test_persona_chat_wire_boundary.py`. Honest alternative: ENFORCE them (make each the single derivation) — deletion-first says delete | ~37 | CONF HIGH |
| HB-3 | `personas.MOTHBALLED_ROLES` + the repo's single real unused import + comment block | dup-authority + plain-unused | 1 hit | delete; no tombstone (never wire) | 9 | CONF HIGH |
| HB-3 | `chat_live_log.chat_live_log_root()` (dup of `capture_chat_live_log_root`) | dup-authority | 2 hits (`__all__` + def) | delete fn + `__all__` row | 8 | CONF HIGH |
| HB-3 | `McpAdmissionOutcome.registered_tool_names` · `mcp_admission.py` (+ the now-dead `tool_names` local) | pass-through field (write-only) | 2 hits (decl + kwarg); `tools/mcp_tool.py::_registered_tool_names` is a different token | delete field + kwarg + local (B9) | 3 | CONF HIGH |
| HB-3 | `AgentCreateOutcome.ok` · `agent_create.py` | test-only property (prod spells `outcome.refusal is not None`) | 3 test asserts | delete + rewrite 3 asserts (or make `.ok` THE authority — one spelling either way) | 4 | CONF HIGH |
| HB-3 | `mcp_admission.py` `role = str(role_from_persona(persona).value)` try-arm mislabelled "defensive" — `AgentRole(StrEnum)` has ONE member `PM="pm"` and `"pm"` is mothballed; `coerce_agent_role` returns `str` for everything else | dead try-arm + a one-member enum reading as a live taxonomy | reading + `personas.py` | replace with the `str` path (−4); do NOT delete `AgentRole` (doc-only mentions in `02-execution-engine.md`) — B1/B2 | 4 | CONF |
| HB-4 | `BoardStore._list_active_cards`, `._list_archived_cards` · `board_store.py` (fresh orphans from ML-8 `3862424998` — both lost their callers to the new `_ordering_cards` chokepoint) | plain-unused | def + `errors.py` docstring (`EXCL`) + plan doc | delete both NOW; the `errors.py:111` prose edit is `EXCL`; suites `test_board_store.py`, `test_board_sync.py` | 6 | CONF HIGH |
| HB-4 | `persona_chat_history._persona_chat_creation_sort_key` | plain-unused | 1 hit | delete | 9 | CONF HIGH |
| HB-4 | `agent_runtime/docs/snapshot_line_count_exception.md` (cites 3,170 lines; `snapshot.py` is 2,505; of the 4 named seams only `_parity_envelope` exists) | moot exception doc | self-referenced only | delete the file (or move the 3,000-line BAR into a test if anyone wants it kept) | 21 | CONF HIGH |
| HB-4 (docs) | broken relative link `agent_runtime/docs/mission_control_chat_parity_stages.md`; `PERSONA_PROFILE_BINDING_AUTHORITY_PLAN_2026-08-16.md` still says `rebind_persona_profile` has "zero callers" (`harness agent set-profile` calls it); `tests/agent_runtime/test_config.py::test_neko_supervisor_uses_configured…` asserts the OPPOSITE of its name → `…_does_not_inherit_head_agent_profile` | doc rot / inverted test name | verified | 3 one-line edits | 5 | CONF HIGH |
| HB-4 `EXCL` | `persona_assignments.safe_assignment_state` (1 hit at `40b0f0c53a`, survived ML-16 B18); `PersonaChatClarifyTicketStore.list_tickets` (`persona_chat_continuity.py`; def + own docstring + `test_persona_lister_refusals.py`) | plain-unused / test-only | as stated | ONE `persona_assignments`+`persona_chat_continuity` `EXCL` stage (with HA-1's caller-side `reason=` edit); `list_tickets` may be the deliberate public read door — judgement | 5+11 | CONF `EXCL` |
| HB-5 RULING | `AgentRuntimeConfig.head_agent_profile` · `config.py` — loaded, shipped on the `runtime_config` snapshot key, read by zero code (escaped the S57 29-field sweep by hanging off the top-level config) | inert wire field | `.head_agent_profile` attr / `getattr` string = 0 tree-wide; wire key present in **all four** `tests/fixtures/stream_frames/*.json` + `MANIFEST.sha256` + all four launcher `test/fixtures/harness_stream/*.json` + the launcher mirror list | **STAGE-STOP**: contract bump + cross-stack golden regeneration; operator ruling, never a mechanical stage | 2 + contract | CONF `STAGE-STOP` |
| HB-5 RULING | `persona_profile_binding.backfill_instance_profile_ids` (138) — no CLI verb, no RPC; propped up solely by `test_instance_profile_backfill.py` (302); its sibling `rebind_persona_profile` DID get wired | unwired lane | 0 prod / 7 test | RULING: land the `--backfill-instances` verb or retire 138 + 302 | 138 (+302) | CONF |
| HB-5 RULING | `mcp_lane.set_entry_point_lane` (deliberately-provided API; neither repo sets `HERMES_ENTRY_POINT_LANE` either) | test-only extension seam | 0 prod / 9 test | keep-or-cut judgement; if cut, move the two suites to the env-var spelling | 12 | CONF MED |
| refuted | `mission_chat_turns._T` TypeVar | — | **REFUTED**: used 3× as the type parameter of `_mutate_session` | DROP | — | REF |

Do-not-cut (recorded): `repo_context.RepoExecutionContext.context_loaded_label` (whole-file KEEP, doc 19), `dispatch_store.get_dispatch` (legitimate test-read seam), `runtime_hud.hud_field` + `volatile_hud_keys` (the declaration-query API the contract test exists to use), `mcp_admission.READ_ONLY_ALLOWLIST_PROFILE = "reviewer"` (the only in-code pointer to the launcher allowlist row); `target_policy` (live via a function-local import in an exec'd part), `usage_ledger` (live via `getattr` string form), `dispatch_max_concurrent` (live via accessor) — one trap class each; the discarded 119-row "no cross-module caller" list is contaminated (`ensure_for_persona`, `drain_once`, `McpAdmission` false positives) — do not resurrect it.

### 1.3 `hermes_cli/harness*` (H-CLI) — census authority = the imported argparse tree (166 paths / 124 harness leaves), not a source grep; suites via `scripts/run_tests.sh <file>` only

| stage | symbol · file | kind | proof | needs | ~lines | conf |
|---|---|---|---|---|---|---|
| H-CLI-1a | `ERROR_EXIT_CODES` — **22** codes that exist only as their own dict row (`lane_not_found`, … full list `audit2/VERIFY-hermes/errorcodes.txt`, regenerate at dispatch) + `goal_not_found`/`realm_not_found` (retired-verb residue; `goal_not_found`'s hint names a retired verb, B2) · `harness_support.py` | plain-unused registry rows (no `code=` site, no exception mapping, 0 launcher readers — the launcher tombstoned the whole `MissionBridgeErrorCode` vocabulary at w6 `39927b5e`) | 70-row table re-censused, git-grep both repos + `scripts/`; **the four `install_*` codes are LIVE** (produced by `scripts/install-mission-control-hermes.ps1`, exit 7, launcher-run) — keep with a producer comment; `provider_auth_expired` is asserted by a launcher test hermes cannot emit — a launcher fix, not a hermes deletion | delete 22 + 2 rows + hint rows; then B7's producer gate as a SEPARATE commit watched red first (add a fake unwired code → red → remove) — the gate must whitelist/read the ps1-produced four; suites `tests/hermes_cli/test_harness_cli.py`, `tests/agent_runtime/test_tombstone_registry.py`, `tests/agent_runtime/test_office_orphan_surface_archive.py` | ~35 | CONF (26→22) |
| H-CLI-1b `EXCL` | `InvalidTransition` / `StaleRun` / `ProofMissing` / `RuntimeRootMismatch` classes (`agent_runtime/errors.py` `EXCL`) + their 4 imports and 4 rows in `_error_code_for_exception` (`harness_support.py`) | island — 0 `raise` sites repo-wide; alive solely to be mapped by one tuple | total refs 4 each (def, import, tuple row, existing `Form.IMPORT` tombstone context) | `harness_support` half NOW; the `errors.py` class deletions + tombstone rows + doc-05 lines `EXCL` | ~16 | CONF |
| H-CLI-2 | `_add_stage42_global_args` — the `if "cursor" in controls:` / `if "since" in controls:` branches · `harness.py` | folded predicate — `controls` defaults `frozenset()` and NO `controls=frozenset({…})` call site (37) names either → `--cursor`/`--since` can never exist (the neighbouring `read --since-offset` is a different flag) | parent re-proved by hand | delete 4 lines; docstring's two dead examples → live ones (`board list --board`, `persona chat history --before`) (B1); suite `scripts/run_tests.sh tests/hermes_cli/test_harness_cli.py -k stage42` | 4 | CONF HIGH |
| H-CLI-2 | `--all-persona-profiles` on `harness install-harness-skills` (self-described "compatibility flag"; dest never read; the same function's docstring forbids exactly this class and the existing gate covers only GLOBAL flags) | fake affordance | dest 0 prod reads; 2 `SimpleNamespace` paddings in `test_persona_skill_policy.py` | delete; suite `tests/agent_runtime/test_persona_skill_policy.py` | 1 | CONF HIGH |
| H-CLI-3 | `harness contracts dump` (parser block + `runtime_commands` handler) | plain-unused verb | 0 invocations both repos + `~/.claude/skills` | cut → `decision_contract_registry.contract_manifest()` becomes test-only-alive AND it is on the `KEPT` list of `test_s54_individual_dead_symbols` *because* `contracts dump` reads it — decide the constant's fate + edit the `decision_contract_registry.py` comment; edit the `test_harness_json_root_observability.py` name ledger in the same commit | 12 | CONF |
| H-CLI-3 | `harness persona instance resolve-chat-turn` (alias; both parsers bind `_cmd_mission_chat_turn_resolve`; launcher emits `'turn-resolve'`) | dup-authority / dead alias | 0 invocations | delete parser block; 2 launcher doc lines say the alias was retired; fixture refresh (B4) | 8 | CONF HIGH |
| H-CLI-3 | `harness workspace actors` (parser + `_cmd_workspace_actors`) | plain-unused verb | 0 invocations both repos | delete; edit the observability name ledger | 12 | CONF HIGH |
| H-CLI-3 (docs) | launcher `test/features/mission_control/fixtures/hermes_cli_contract.json` — 14 paths + 1 flag stale (152 → 166; ML-16 added `profile delete --force-unverified-writers`; additions only, the offline gate is blind to them) | stale committed fixture | re-dumped with the launcher's own `dump_hermes_cli_contract.py` | refresh AFTER the H-CLI-3 verb cuts (or it is stale twice); note in the fixture README that ADDITIONS drift silently; launcher `flutter test test/features/mission_control/harness_capability_argv_test.dart` | fixture | CONF STALE-detail |
| H-CLI-5 RULING | `harness realm bind-server` (0 invocations — but the lane's "reachable via `realm adopt --server`" rationale is FALSE: adopt filters, it does not bind; after the cut `server_id` is write-once-at-create) + `harness workspace add-agent`/`remove-agent` closed loop (`workspace.agent_ids` IS read by `default_scope`; `WorkspaceStore.add_agent/remove_agent` callers = these verbs only, 0 tests) | uninvoked verbs whose deletion changes a data model's mutability | as stated; `test_entity_row_characterization.py` names `bind-server` in prose | ONE "write-once-at-create" ruling for both; `store.py` follow-ons → HA-5 `EXCL`; keep `RealmStore.bind_server` if `adopt_realms`/tests need it | 14 + 55 | CONF MED |
| H-CLI-5 RULING | `_installed_pet_gallery_row` constant empty keys `submittedBy`/`petJsonUrl`/`zipUrl` (launcher `petdex_client.dart` reads `slug/displayName/installed/description/kind/spritesheetUrl` only) | write-only wire fields | launcher grep | shape parity with the remote gallery row is the only argument to keep — ruling | 3 | CONF MED |
| **NEW-1** | `_append_persona_operator_turn`, `_append_persona_assistant_text`, `_persist_persona_chat_row`, `_update_persona_chat_token_counts`, `_positive_int_or_zero` · `harness_parts/persona_commands.py` — the free-floating persona-chat persistence lane | test-only-alive **island** (the only unreachable defs in the whole CLI surface: 357/362 reachable); orphaned by S70 `6b9a27a741`; the live twin for the MIRROR is `agent_runtime.chat_live_log.mirrored_persona_chat_append` (used by `continuity.py`) — keep that; neighbours `_ensure_persona_chat_session`, `_persona_chat_session_owner`, `_redact_persona_chat_text` ARE live | prod refs = 0 outside their own bodies (+1 comment) — parent re-proved: every "prod" hit is intra-island; test readers `tests/hermes_cli/test_persona_chat_session.py` (353, largely retired), `test_agent_chat_log_path.py`, `tests/agent_runtime/test_persona_assignments.py` (2 lines), `test_relay_session_lifecycle.py` (3), `tests/hermes_cli/test_c8_one_order_guards.py::test_assistant_append_lost_its_finish_reason_seam`, `test_mission_chat_usage_single_writer.py` (stays green; its premise becomes moot) | delete 5 defs; trim/rewrite 6 test files; `Form.CODE` rows; **own stage** (~184 + ~250 test — over the cap if bundled); suites the six files + `tests/hermes_cli/test_harness_cli.py` | ~184 (+~250 test) | NEW HIGH |

### 1.4 Periphery — fork-owned files outside `agent_runtime/` and `harness*` (H-PERIPH)

| stage | symbol · file | kind | proof | needs | ~lines | conf |
|---|---|---|---|---|---|---|
| H-P1 | `scripts/certification_ladder.py` — whole file (drives a `harness burn-in` verb that no longer exists; the unreaped twin of `scripts/cert_streak.py`, deleted at S6) | plain-unused | 0 refs in hermes (incl. `website/`), launcher, backend; the 29 "burn" hits are prose | delete; **widen `tests/scripts/test_cert_streak.py`** from a single-name pin to both driver module names (rename the test "drivers", plural) in the same commit (B1); `Form.MODULE` row | 57 | CONF HIGH |
| H-P1 | `tools/agent_chat_dispatch.py::shutdown_dispatch_executor` (docstring: "tests; never on a live lane" — no test calls it) and `::is_supervised_here` (the never-adopted sibling of the live `supervised_dispatch_ids`, born in the same commit) | plain-unused exports | 2 refs each (`def` + `__all__`); launcher 0 | delete + `__all__` rows; 2 `Form.CODE` rows; suites `tests/agent_runtime/test_agent_chat_dispatch.py`, `test_dispatch_store.py`, `tests/scripts/test_cert_streak.py`, `test_tombstone_registry.py` | ~19 | CONF HIGH |
| H-P2 | `tools/board_tool.py::_resolve_board_target` — the `if task_id:` branch + the `paths` import used only inside it (`TaskStore` is aliased to `TaskStoreStub`, whose `.get()` is `-> NoReturn` and always raises); the module docstring's dead rung + the error string; **`BOARD_CARDS_SCHEMA` and its mirror `tools/tool_full_descriptions.py` still promise a "linked goal" field removed at S3/S4** (a test asserts its absence — the wire description lies to the model) | structurally unreachable branch + lying schema text | `git cat-file`: `board_tool.py` is NOT upstream (doc 17 §1/§3 agree) | delete branch + import; strike the rung and `linked goal` in both strings (deletion-first; a test pins brief-vs-mirror parity); stop-and-report if any `_resolve_board_target` return value changes on the active-workspace path; suites `tests/agent_runtime/test_board_agent_tools.py`, `test_board_store.py`, `tests/tools/test_t6b_brief_descriptions.py`, `tests/hermes_cli/test_harness_json_root_observability.py` | ~12 | CONF HIGH |
| H-P3 (docs) | `hermes_cli/gateway_home_receipt.py` `RESOLUTION_*` comment claims `main.py` imports the constants — `main.py` deliberately hardcodes all four inline and says so (four wire literals in two files with no gate); `scripts/upstream_sync_gate.py::AGENT_TOOL_SEAMS` names `tools/agent_chat_tool.py` but not `board_tool.py` / `agent_chat_dispatch.py` (a sync touching either silently skips the launcher lane and reports PASS); `DEFAULT_LAUNCHER_ROOT` + `backend_postgres_proof.py` hardcode personal machine paths as committed defaults; `hermes_cli/windows_env.py` docstring states a sync contract the fork is forbidden to honour | stale rationale / trigger-set drift / machine paths | verified | rewrite the rationale + add a four-literal pin against `main.py`; add the two paths (or prefix-match); env-var with fallback; retarget the docstring; suites `tests/hermes_cli/test_gateway_home_receipt.py`, `tests/scripts/test_upstream_sync_gate.py`, `tests/scripts/test_backend_postgres_proof.py`, `tests/hermes_cli/test_dep_ensure.py`, `tests/cli/test_auth_noninteractive.py` | ~11 | CONF |
| H-P4 RULING | (i) revisit ruling R-3: after H-P2 the only `TaskStore` import is gone → `agent_runtime/task_store_stub.py` (~60) can retire; its "DO NOT DELETE" docstring rests on the disproved premise that `board_tool.py` is upstream-owned; blast radius = `agent_runtime/store.py` import (`EXCL`) + **4 pinning tests** (not "one import line"). (ii) `mobile_core/` (4,443 lines): only functional reader is its own test suite; the launcher's `tool/mobile_core/*.dart` consumers run from operator runbooks; **zero CI jobs in either repo** — add a tier/job or declare it archived (report-only). (iii) `agent/charsheet/` `palette_colors`, `SheetSpec.row_by_key`, `DirectionScheme.is_mirrored` — test-only but shipped yesterday and live-wired: HOLD, re-check in ~30 days | rulings | as stated | operator | 60 / 0 / 19 | RULING |

Fork-added blocks inside upstream files (report-only, never edited): `hermes_cli/kanban_db.py` (+562 — verifier: 10 defs, all one live-rooted chain, no dead fork code), `gateway/run.py` (+254), `gateway/kanban_watchers.py` (+148), `gateway/slash_commands.py` (+80), `plugins/dashboard_auth/*` (+13 each), `platforms/feishu/adapter.py` (+58) — not read below def level.

## 2. Readability rows (deletion-first; symbols + files)

| # | file · symbol | issue | proposed change | ~lines | conf |
|---|---|---|---|---|---|
| R1 | `REFACTOR_DEBT_AUDIT_2026-08-17.md` §0 Correction 4, §1.7, §5; `SCOUT_HERMES_SWALLOW_AUDIT_2026-08-17.md`; doc 17's `board_tool.py` row wording | the load-bearing "resolve_conflict never landed / seven RPC methods / any parity claim…" sentences are false since `32a392364b` | dated superseded notes; "eight methods, 7 of 8 launcher-consumed; the CLI-only verb is `office.actor.restore`" — one batched doc commit | ~10 | HIGH |
| R2 | `harness.py::_add_stage42_global_args` docstring | justifies the design with two DEAD examples (`--cursor`/`--since`) | replace with live ones | 2 | HIGH |
| R3 | `harness_support.py::_error_hint` `goal_not_found` row; `ERROR_EXIT_CODES` whole table | a hint that misdirects to a retired verb; a registry with no producer gate | delete with H-CLI-1a; add the sibling producer gate (every key reachable from a `code=` site / exception map / the installer ps1) | ~16 | HIGH/MED |
| R4 | all six `harness_parts/*.py` — the identical 7-line "Explicit import header. Still exec'd into harness…" paragraph | duplicate helper prose ×6 | keep ONCE in `harness_support.py`'s module docstring, one-line pointer in each part | ~42 | MED |
| R5 | `harness.py` `_cmd_workspace_actors` / `_realm_row` / `_office_actor_row` / `_board_row` / `_card_row` … | naming hides that after S48 these are pure delegations to `office_summary_row`-class helpers | rename or inline (~25 renames) — after the other session's `harness_parts` work is quiet | 25 | MED |
| R6 | `snapshot.py` comment "Injected stores (tests, doctors) must observe…" | asserts a guarantee the code cannot give | becomes true when HA-1 lands | 0 | HIGH |
| R7 | `serve_registry.py`/`serve_socket.py` (`_atomic_write_json` vs `_write_json_atomic`); `read_model._optional_text` vs `stream._text` | naming by word-order inversion hid three duplicate triplets | rides HA-2; `serde.py` becomes the home for wire-shape coercion (B10) so the NEXT helper has one | 0 | HIGH |
| R8 | `realm_sync.py` `RealmMembershipProvider.authorize(self, realm, action)` | unused param on an authorization boundary | one-line comment; no code change | 1 | MED |
| R9 | `skill_promotion.py` `PLAN_ACTIONS` comment; `refuse_ambiguous_source` | false claim in a comment; phantom action | delete with HB-2 | 3 | HIGH |
| R10 | `personas.py` `class AgentRole(StrEnum)` (one member, retired) | reads as a live taxonomy | doc it as retired residue (do not delete — doc mentions in `02-execution-engine.md`) | 2 | MED |
| R11 | `pyproject.toml` `[tool.ruff.lint] select = ["PLW1514", "F821"]` | F401 off → unused imports invisible to CI (this pass found exactly one) | add `F401` with per-file-ignores for upstream dirs (`agent/**`, `gateway/**`, `tools/**`, `tui_gateway/**`) — RULING | 1 | MED |
| R12 | `tests/agent_runtime/test_config.py::test_neko_supervisor_uses_configured…` | test name asserts the opposite of its body | rename `…_does_not_inherit_head_agent_profile` | 1 | HIGH |
| R13 | `board_tool.py` docstring rung + error string; `BOARD_CARDS_SCHEMA` / `tool_full_descriptions.py` "linked goal" | dead rung documented as live; wire description lies to the model | with H-P2 | 4 | HIGH |
| R14 | `gateway_home_receipt.py` `RESOLUTION_*` rationale; `upstream_sync_gate.py` `AGENT_TOOL_SEAMS`; machine-path defaults; `windows_env.py` docstring | stale rationale / trigger drift / personal paths | H-P3 | ~11 | HIGH/MED |
| R15 | `hermes_cli_contract.json` (launcher fixture) README | additions drift silently — the offline gate cannot see them | say so; refresh after H-CLI-3 | 2 | HIGH |
| R16 | `mcp_admission.py` `McpAdmissionOutcome` field ordering (`registered_tool_names` un-commented between two `#:` fields) | convention gap | dies with HB-3 | 0 | MED |

## 3. Stage plan — ordered, with test plans

All stages: fresh worktree cut from `main`, one implementer, explicit-path commits, `git fetch origin && git rebase origin/main` then ff-only, never force; per-file runner for `tests/hermes_cli`; every removal watched red first via its tombstone row; **full `python -m pytest tests/agent_runtime/ -q` (parent, background shell, tail to file) at the completion checkpoint of every stage that touches `agent_runtime/`**; golden check `tests/fixtures/stream_frames/MANIFEST.sha256` unchanged after every stage; live-leak check `X:/Eternia/.hermes/agent-runtime/office/ws_office_patch_test/actors/personainst_qa_agent_0001.json` still `revision 67`. Re-read the launcher ledger LIVE STATE before dispatch and before landing.

**Tier 1 — mechanical, no rulings, no `EXCL`:**
1. **HB-1** `risk_flags.py` island + `serde` arm (~97; 2 registry rows) — `scripts/run_tests.sh tests/agent_runtime/test_models_serde.py tests/agent_runtime/test_tombstone_registry.py tests/agent_runtime/test_persona_chat_history_curation.py`, then the full checkpoint.
2. **HA-2** one authority per helper (~64 incl. the three NEW third members; zero behaviour change) — home the survivors in `serde.py`/`paths.py` first, delete in the same commit; suites `test_read_model*.py`, `test_stream*.py`, `test_snapshot*.py`, `test_serve_registry.py`, `test_serve_socket*.py`, `test_realm_sync*.py`, `test_paths.py`, `test_runtime_hud*.py`, `test_skill_promotion.py`, `test_board_store.py`; goldens unchanged (bodies byte-identical).
3. **HA-1** pass-through + dropped params (~17; the `emit_persona_instance_remove` caller edit deferred to the `EXCL` stage) — `test_snapshot_coalesce.py`, `test_observability.py`, `test_state_patches*.py`, `test_stream_patch.py`, `test_s28_status_observe_shrink.py`.
4. **HA-3** dead fields + `dropped_by_design` (~18; no contract bump — none on any wire allowlist or golden) + WIRE `updated_by` — `test_realm_sync_publish*.py`, `test_models_serde.py`, `test_s53_lane_write_lane_removal.py`, `test_parity.py`, `test_running_work.py`, `test_office_store*.py`.
5. **HB-2** the four vocabularies (~37) — `test_dispatch_session_policy.py`, `test_skill_promotion.py`, `test_skill_publishability.py`, `test_persona_chat_wire_boundary.py`.
6. **HB-3** one spelling per question (~28) — `test_personas.py`, `test_persona_runtime_fake.py`, `test_chat_live_log.py`, `test_mcp_admission*.py`, `test_agent_create_service.py`, `test_tool_visibility.py`.
7. **HB-4** orphans + rot (~40; the `errors.py` docstring line waits) — `test_board_store.py`, `test_board_sync.py`, `test_persona_chat_history*.py`, `test_turn_state_vocabulary.py`, `test_config.py`.
8. **H-CLI-2** the branches and flag that can never fire (~5) — `scripts/run_tests.sh tests/hermes_cli/test_harness_cli.py -k stage42`, `tests/agent_runtime/test_persona_skill_policy.py`.
9. **H-CLI-1a** exit-code taxonomy tells the truth (~35; then B7's gate as its own red-first commit) — `tests/hermes_cli/test_harness_cli.py`, `tests/agent_runtime/test_tombstone_registry.py`, `tests/agent_runtime/test_office_orphan_surface_archive.py`.
10. **H-P1** (~76 + tombstone widening), **H-P2** (~12), **H-P3** (~11 docs) — suites in §1.4.
11. **HA-4 + R1** one batched doc commit (~20 across 4 docs + `source_grep_debt.txt` header).
12. **NEW-1** persona-chat persistence island (~184 + ~250 test; own stage) — the six named test files + `tests/hermes_cli/test_harness_cli.py` via the per-file runner; `Form.CODE` rows.

**Tier 2 — after Tier 1, cross-stack fixture refresh:**
13. **H-CLI-3** three uninvoked verbs (~32) + the observability name-ledger edits + the `contract_manifest` KEPT decision — then the launcher fixture refresh (166 paths + the ML-16 flag) as a launcher commit + `flutter test test/features/mission_control/harness_capability_argv_test.dart`.

**Tier 3 — `EXCL`-tagged (lanes are EMPTY now; re-read the traffic light):**
14. **HA-5** `store.py` (`list_open_with_closed_count`, `_safe_model_id` third copy, + `bind_server`/`add_agent`/`remove_agent` follow-ons IF H-CLI-5 rules them out) — `test_store.py`, `test_observability.py`.
15. **HB-4-EXCL** `persona_assignments.safe_assignment_state` + HA-1's caller-side `reason=` + `list_tickets` judgement — `test_persona_assignments.py`, `test_persona_lister_refusals.py`; B1 fence byte-unchanged (sha256 above).
16. **H-CLI-1b** `errors.py` never-raised classes + tombstone rows + doc-05 lines + the `errors.py:111` docstring edit from HB-4.

**Tier 4 — rulings (§4):** HB-5 (`head_agent_profile` STAGE-STOP; `backfill_instance_profile_ids`; `set_entry_point_lane`), H-CLI-5 (`bind-server`/`add-agent`/`remove-agent` write-once-at-create; pet-gallery keys), H-P4 (R-3 revisit + `task_store_stub.py`; `mobile_core/` gating; charsheet hold), R11 (`F401`).

## 4. Rulings owed to the operator (recommendation in **bold**)

1. `head_agent_profile` — accept a contract bump + cross-stack golden regeneration to drop an inert config field? (**defer**: 2 lines are not worth a golden landing; record it in doc 19 as debt with the trigger "next contract bump").
2. `backfill_instance_profile_ids` — land `harness agent set-profile --backfill-instances` or retire 138 + 302 test lines (**land the verb** — the plan doc still lists the gap as open).
3. `set_entry_point_lane` — keep the escape hatch (**cut**; move the two suites to the env-var spelling).
4. `realm bind-server` / `workspace add-agent|remove-agent` — is `server_id`/`agent_ids` write-once-at-create? (**yes → cut all three verbs + the store methods**).
5. `contract_manifest` after `contracts dump` goes (**keep the constant, re-word the KEPT pin's rationale**).
6. Pet-gallery empty keys (**keep for shape parity; comment**).
7. R-3 revisit → `task_store_stub.py` retirement (**yes, after H-P2, with the 4 pinning tests**).
8. `mobile_core/` — CI tier or archived? (operator).
9. `pyproject.toml` `F401` on with upstream per-file-ignores (**yes**).
10. `updated_by` on `archive_orphaned_surface` — wire or drop (**wire**).

## 5. What was NOT swept

- Intra-function dead branches inside live handlers of `harness.py` (~50 command bodies), `serve.py` (3.4k), `persona_commands.py` (6.4k) — reachability was at def granularity; the only intra-function claims re-proved are H-CLI-2's. `serve.py`'s private constant surface (`_DRAIN_*`, `_FINGERPRINT_*`, `_READ_CACHE_*`) not censused.
- `core_cache.py` (`EXCL`, 1,450) and `realm_sync.py` (2,381) internals; `serve_socket.py`/`serve_office_subscriptions.py` lock/lease semantics; `prompt_observability.py` context-assembly logic beyond the two-authority check.
- Wire-token sweeps NOT done: receipt kinds, the `correlation_id` vocabulary, terminal-envelope grant tokens, redaction key lists, `machine_roots` template keys (event kinds, RPC names, frame types, fold tokens, read-model tables, capability ids, exit codes, `counts`/`requirement_failure`/`collapsed_dock_regions` WERE swept).
- `tests/agent_runtime/` (332 files) and `tests/hermes_cli/` as SUBJECTS (dead helpers, duplicated fixtures, change-detector tests — the launcher ledger's B23(ii) `_Fake*`/`_Stub*` sweep is the follow-up); the 12 fork-created test files under `tests/{cli,tools,gateway}` for vacuous assertions.
- `agent_runtime/docs/*.md` beyond link integrity + `snapshot_line_count_exception.md`; upstream `profile *` (11 of 12 leaves have no launcher emitter — recorded, no claim); the shared skills root outside both repos; `website/`; `mobile_core/` internals; `plugins/` fork deltas; `gateway/*` fork blocks below def level.
- H-CLI's 334-flag count; H-ART-A's `census.json`; H-ART-B's `code_index.json` beyond the verifier's spot checks (it produced one false "1 hit" — treat other decl-only claims from it as verified only where §1 shows the verifier's own count).
- **No test suite was run in Phase 1** — every "goes red / stays green" is a reading of the source; the implementer proves it at the completion checkpoint.

## 6. Cross-stack rows for the launcher plan

- **NEW-2** `runtime.office.unsubscribe` (`serve_rpc.py`, `0a1e63687d` 2026-08-15) has zero launcher callers (`lib` = 0; the subscribe lane's `dispose()` never sends it; no `kMissionOffice…Unsubscribe…` constant in `mission_office_rpc.dart`) — a launcher wiring row, or an explicit "close-socket-only" ruling recorded on both sides. Not a hermes deletion.
- `provider_auth_expired` — a launcher test asserts a code hermes cannot emit (launcher fix).
- `hermes_cli_contract.json` refresh after H-CLI-3 (launcher commit).
- `MissionRunningWork` `counts` and the singular `requirement_failure` envelope are emitted here and unread there (launcher rulings 3 in its §6).
- `head_agent_profile` regeneration, if ever taken, mirrors into `test/fixtures/harness_stream/` + both manifests (parent-owned cross-stack landing).

## 7. Refuted (so nobody re-derives them)

`mission_chat_turns._T` (3 uses as a type parameter); the four `install_*` exit codes (installer PS1 producer); H-CLI A7's "reachable via `realm adopt --server`" rationale (adopt filters); "all 8 RPC methods launcher-consumed" (7 of 8); H-ART-A's `_accessible_skills_context`/`available_skills_context` duplicate-authority suspicion (they compose); the `kanban_db.py` fork block (all one live-rooted chain); `target_policy`, `usage_ledger`, `dispatch_max_concurrent` (function-local import / `getattr` string / accessor). Kept-deliberately from pass 1 unchanged: the `repo_context` creator trio, `run.closed`, `return-summary --proof-id`, `moa.*`, `office.actor.restore`, `contract_manifest`/`MODEL_INVALID_OUTPUT`/`MAX_KEY_LENGTH` KEPT pins.
