# EG-0.2 — receipts before belief (2026-08-17)

> **Home.** `docs/agent-runtime-harness/`. Discharges stage **EG-0.2** of
> `MISSION_CONTROL_ENTERPRISE_PLAN_2026-08-17.md` (Plan EG §3, Phase 0), which merged **RD-0**
> and the surviving half of **TC-0**.
>
> **Read at.** hermes `d0a3b63c7d8c6c2ff428c384a5d42f5bb5546804` (main, clean) · launcher
> `ec852fc95cd996f552afd211c1ec614c7d11e0f7` (main; dirty tree — only agent-memory,
> `.vscode/settings.json`, `pubspec.lock/yaml` and two untracked scratch paths; no
> `lib/` or `test/` file modified, so every grep below is a HEAD grep).
> **Head drift from the plan:** Plan EG was written against hermes `0e125dcf64` and launcher
> `6a121cbe9`. Both have moved. Every line cite in this document is re-derived at the SHAs
> above; where the plan's cite drifted, the drift is named.
>
> **Strictly read-only.** No test ran, no code changed, no hermes process spawned, nothing
> under `X:/Eternia/.hermes/` written. Logs were read.
>
> **Concurrency note.** EG-0.1 landed in the hermes worktree *during* this pass (uncommitted
> edits to `office_store.py`, `harness.py`, `paths.py`, `decision_contract_registry.py`,
> `harness_parts/office.py`, five test files, plus
> `tests/agent_runtime/test_no_midtest_monkeypatch_undo.py` and
> `test_office_orphan_surface_archive.py`). Every hermes line cite below was therefore
> **re-verified against the `d0a3b63c7d` blob with `git show`**, not against the worktree. A
> reader re-running these greps on a dirty worktree will see `harness.py` cites shifted by +9
> (EG-0.1 inserts 9 lines at `:776`); `office_store.py:262-264` and all of
> `serve_office_subscriptions.py` are unshifted.
>
> **Log sources.** L = launcher diag log
> `C:/Users/beast/AppData/Local/Temp/eternia_launcher_diag.log` (16,935 lines, 15 sessions,
> `2026-08-16T14:53:09Z` → `2026-08-17T12:30Z`; timestamps are LOCAL = UTC−4).
> S = serve-child log `X:/Eternia/.hermes/profiles/base/logs/agent.log` (1,755 lines) + its
> rotations `.1` / `.2` and `errors.log`.
> **R = the transport receipt ring**
> `C:/Users/beast/AppData/Roaming/com.arcadialabs/eternia_launcher/diagnostics/mission_transport/receipts.jsonl`
> (972 lines, `2026-08-14T00:26Z` → `2026-08-17T12:30Z`) — **not named in the stage brief, and
> load-bearing:** it is the ONLY channel `patch_gap` is written to (§4.1).

---

## 0. Verdict up front

| Priced item | Receipt verdict |
| --- | --- |
| **EG-1.3 headline** (does a folder-only batch promote un-coalesced?) | **NOT DECIDABLE FROM THE FIELD — and the absence is itself the finding.** `office_surface` appears in **0 of 85** entity-tagged fold lines in L. The absence is *explained at both heads*: a folder-only `patch` batch is dropped by `office_patch_sink`'s scope predicate before it can become a frame. The defect is **CODE-CONFIRMED at a severity ABOVE the plan's wording** — not "promotes un-coalesced" but "promotes, then vanishes with neither patch nor resync". §1 |
| **EG-4.0** (`laneAbsent` window's ordinary-gesture share) | **0 % post-BW (0 of 5 gestures).** The window's writes were **HELD and then delivered on RPC** — `hold: 5 staged, lane=waiting` → `write lane: 5 rpc, 0 cli`. BW-L6's hold *already implements EG-4.0's own design*. **EG-4.0 closes as measurement; TC-3 (EG-4.3) is unblocked for free.** §2 |
| **EG-5.1** (refusal exposure) | **12 `REVISION MISS`, 12/12 `lane=rpc guarded=false`, 0 guarded refusals** (nothing sends the token — see §3.1). **Contradicts the plan:** the rate does not merely "decide the rollout note". All 12 are page-open re-upserts predicting revision **1** against a server at **2–30**; arming the guard on that path refuses **12 of 12**. §2.3 |
| **EG-0.3 Class-A reap 1** — three `--expect-revision` argv lowerings | **CONFIRMED DEAD.** Plus a precision correction: there are **five** such lowerings at HEAD; two are **LIVE board verbs** and must not be touched. §3.1 |
| **EG-0.3 Class-A reap 2** — `_fetch_usage_lane` fall-through | **CONFIRMED DEAD**, by a closed filter-only chain. Plan cite `:3355-3357` is stale → **`:3702-3704`** (drift +347). §3.2 |
| **EG-0.3 Class-A reap 3** — the two test-only fold copies | **CONFIRMED `@visibleForTesting`, zero production callers.** Test re-point surface: **1 file** for one copy, **8 files** for the other. §3.3 |
| **EG-1.1 / EG-2.2 urgency** (`patch_gap` / `fold:gap`) | **1 gap in ~4 days**, 0 post-BW. Urgency priced **LOW on this evidence**. But the grep the brief specifies returns a **false zero** on the diag log (§4.1). §4 |

---

## 1. R0-a — the folder-change batch and the page-open batch

### 1.1 The decode channel

The diag log's `[MissionFold]` lines **name their entity classes**, which makes batch
composition decodable without touching the event log:

```
L:1361 [17:39:58.919] INFO eternia.mission.fold — [MissionFold] applied 2 of 2 rows at 88707753->88711665 (office_actor x1, persona_instance x1)
L:1380 [17:40:06.163] INFO eternia.mission.fold — [MissionFold] REFUSED gap: base=88714711 held=88717592 — 2 rows, 0 applied (office_actor x1, persona_instance x1); discontinuous base, re-hydrating
```

Emitter: `mission_read_model.dart:177 _describeEntityCounts`.

### 1.2 Census of every entity-tagged batch in L (99 `[MissionFold]` lines; 85 carry a composition)

| Composition | Count |
| --- | --- |
| `(office_actor x1, persona_instance x1)` | 42 |
| `(office_actor x1)` | 31 |
| `(persona_instance x1)` | 6 |
| `(office_actor x2, persona_instance x1)` | 5 |
| `(office_actor x2)` | 1 |

Distinct entity tokens across all 85: **`office_actor` (79), `persona_instance` (53)**.
**`office_surface`: ZERO.** Corroborated in R: 79 `fold_applied` receipts, details only
`entries:1` (42) / `entries:2` (32) / `entries:3` (5) — the ring records a count, never a class.

**A folder-only batch does not exist in the operator's logs. Absence recorded, per the brief.**

### 1.3 Why it is absent — the mechanism, at both heads

The absence is not "no folder write happened". Folder writes were attempted **five times** in
the window (§2.2): four refused pre-BW, **one landed on RPC post-BW** at `L:16926`
(`write lane: 5 rpc, 0 cli` against `hold: 5 staged` — 4 actor upserts + 1
`office.surface.update`; no `office.surface.update REFUSED` line follows it). The store offset
moved `88861422 → 88867005`. **No `office_surface` fold line followed.**

The launcher **does** declare the tokens, so coverage is not the gate:

```
launcher lib/features/mission_control/data/mission_read_model.dart:159-167
const List<String> kMissionFoldDeclaredEntities = <String>[
  'persona_instance', 'incident',
  kMissionFoldOfficeActorEntity,
  kMissionFoldOfficeActorLifecycleCapability,
  kMissionFoldPersonaInstanceCreateCapability,
  kMissionFoldOfficeSurfaceEntity,          // 'office_surface'
  kMissionFoldOfficeSurfaceCapability,      // 'office_surface_fold'
];
```

hermes covers the paired event and gates the row on that same token
(`patch_coverage.py:179 OFFICE_SURFACE_FOLD_CAPABILITY`, `:512-516`), and the store emits the
row (`office_store.py:262-264 emit_office_surface_patch`, entity name
`state_patches.py:929 OFFICE_SURFACE_ENTITY = "office_surface"`). So the coverage authority
**promotes** a folder-only batch for this client.

The sink then drops it. Both arms test `office_actor` and only `office_actor`:

```
hermes agent_runtime/serve_office_subscriptions.py:445-456   (the COVERED / `patch` arm)
        rows = [patch for patch in frame.get("patches") or [] if isinstance(patch, dict)]
        in_scope = any(
            patch.get("entity") == OFFICE_ACTOR_ENTITY
            and str(patch.get("id") or "").startswith(prefix)
            for patch in rows
        )
        if not in_scope:
            return
```

```
hermes agent_runtime/serve_office_subscriptions.py:311-316   (the UNCOVERED / `delta` arm)
        if event_type == STATE_PATCHED_EVENT_TYPE:
            if payload.get("entity") == OFFICE_ACTOR_ENTITY and str(
                payload.get("id") or ""
            ).startswith(prefix):
                return True
```

A folder-**only** `patch` batch therefore sets `in_scope = False` and returns at `:456`:
**no patch forwarded, no resync emitted, nothing logged.** (The uncovered `delta` path is safe
by a different clause — `:301-310` catches `office.*` domain events by `workspace_id` — so the
hole is confined to the COVERED lane, i.e. it opened exactly when WV-H3 made
`office.surface.updated` coverable without widening the sink. RD-H1's prose, now with the
predicate quoted at HEAD.)

### 1.4 The page-open batch, decoded (post-BW, `L:16916-16932`)

```
[08:28:48.482] [MissionOfficeSubscribe] unavailable (start): laneAbsent — office stays on runtime.office.get
[08:28:48.761] [MissionOfficeRpc]       on the snapshot lane (laneAbsent)
[08:28:51.035] [MissionOfficeRpc]       STILL on the snapshot lane (laneAbsent) after 2 consecutive reads
[08:28:51.600] [MissionOfficeWrite]     hold: 5 staged, lane=waiting                 ← BW-L6's hold
[08:28:54.665] [MissionOfficeSubscribe] subscribed (start) at baseline 88861422 — 8 items, folded 0, resubscribes 0
[08:28:54.766] [MissionOfficeWrite]     office.actor.upsert REVISION MISS actor=…backend_dev…29fdd71a lane=rpc guarded=false predicted=1 server=11
[08:28:54.860]                          … dev_agent_3ebfce41   lane=rpc guarded=false predicted=1 server=12
[08:28:54.981]                          … neko_supervisor…f6f7a51b lane=rpc guarded=false predicted=1 server=30
[08:28:55.029]                          … qa_agent_9c8a382f    lane=rpc guarded=false predicted=1 server=11
[08:28:55.061] [MissionOfficeWrite]     flush: 4 upserted, 0 removed, 0 refused
[08:28:55.061] [MissionOfficeWrite]     write lane: 5 rpc, 0 cli
[08:29:17.806] [MissionOfficeSubscribe] resubscribe #1 (push:full_core) in 250ms (cap 5 per 60s)
[08:29:18.187] [MissionOfficeSubscribe] subscribed (push:full_core) at baseline 88867005 — 8 items, folded 0, resubscribes 1
```

Composition: **11 desk actors staged → 5 dirty (4 actor upserts + 1 surface write) → 5 RPC
calls → 4 upserted, 0 refused.** Held 3.1 s, delivered whole on the lane-up edge.

**Honest limit on §1.3's field half.** The 22.7 s hydrate was still building through this whole
window (`S: 2026-08-17 08:29:17,703 snapshot_build reason=hydrate elapsed_ms=22687
offset=88867005`), so the `folded 0` + `push:full_core` recovery is *consistent with* the sink
drop but does not isolate it: with no producer, no patch frame could have been emitted for any
entity. The code path in §1.3 is the proof; the field is corroboration only.

### 1.5 WV-0's surviving A-1, discharged

Plan EG §7 folds WV-0's A-1 (boot-batch composition) into R0-a. **Answer:** the boot batch is
**not** the 23-event startup batch the register still describes. It is a **5-write launcher
flush** (4 actor upserts + 1 surface write) against a batch of 11 staged actors, and post-BW it
rides RPC whole. See §5 for the striking edit this licenses.

### 1.6 What this does to Plan EG's sequencing

**A contradiction to flag.** Plan EG §2 (Class B) and §4.3 make the stream-child reap
conditional on *"R0-a confirming folder-only frames arrive on the push lane."* Per §1.3 they
**cannot** arrive on the push lane at HEAD — the sink returns before emitting. That
confirmation is a **post-condition of EG-1.3, not a pre-condition measurable now**, and
obtaining it requires a deliberate folder-change gesture against a live, lane-up runtime — a
WRITE, outside EG-0.2's read-only charter. **Recommended adjustment:** restate §4.3's clause as
"EG-1.3 live AND a post-EG-1.3 live folder gesture yielding an `(office_surface x1)` fold line",
and schedule that gesture as an explicit acceptance step of EG-1.3 rather than a gate EG-0.2
could have cleared.

---

## 2. R0-b — `write lane:` / fallback reasons / `REVISION MISS`

### 2.1 The receipt-key rename

Two keys carry the same fact and both must be grepped: **`upsert lane:`** (27 lines, older) and
**`write lane:`** (9 lines, current). The brief's grep for `write lane:` alone finds 9 of 36.
Counters are **per flush**, not cumulative (S11 emits `0 rpc, 5 cli` four times, not 5/10/15/20).

### 2.2 Tally, bracketed by session

BW marker: the boot that logged `ready_to_authoritative=24614` (`L:16930`), i.e. session
**S15** (`===== diag log opened 2026-08-17T12:23:35.113369Z (pid=11624)`, `L:15470`). Every
earlier session is **PRE-BW** — the key is absent from all 14 of them.

| Session | opened (UTC) | lane receipts | rpc | cli | fallback |
| --- | --- | --- | --- | --- | --- |
| S2 | 08-16T15:28:37 | 2 | 11 | 1 | `laneAbsent` ×1 |
| S4 | 08-16T21:39:28 | 5 | 5 | 0 | — |
| S6 | 08-16T23:08:41 | 15 | 21 | 1 | `laneAbsent` ×1 |
| S10 | 08-17T00:47:20 | 5 | 5 | 0 | — |
| **S11** (pre-BW) | 08-17T09:48:16 | 8 | 4 | 20 | **`laneAbsent` ×4** |
| **S15** (POST-BW) | **08-17T12:23:35** | **1** | **5** | **0** | **none** |
| **whole log** | | **36** | **51** | **22** | **`laneAbsent` ×6** |

**`laneAbsent` is the ONLY fallback reason recorded anywhere in the log.** Zero
`serve_absent`, zero anything else; the string `fallbackReasons` never appears in L (the
receipt renders as `(fallback: <reason>)`).

**Post-BW `laneAbsent` share of ordinary gestures: 0 of 5 = 0 %.**

**All 22 CLI write attempts sit on the 6 `laneAbsent` lines.** CLI attempts with reason ≠
`laneAbsent`: **0**. This is TC-3's exit criterion for the argv office write arms, already met
on this evidence — subject to the window being agreed formally in EG-4.3.

**The pre-BW window's real cost, and it is worse than the plan states.** Plan EG §2 describes
the argv arms as *carrying* ordinary gestures inside the window. In S11 they **failed** them:

```
L:5216 [05:48:26.700] office.surface.update REFUSED actor=ws_codex-… lane=cli kind=unspecified: Not sent — Mission Control is still verifying the Hermes runtime…
L:5217 [05:48:26.700] flush: 0 upserted, 0 removed, 5 refused
L:5218 [05:48:26.700] write lane: 0 rpc, 5 cli (fallback: laneAbsent)
```

Four flush cycles (`05:48:26.700`, `:28.555`, `:29.610`, `:31.066`) × 5 writes = **20 refusals,
0 landed.** Every write refusal in the entire 16,935-line log is one of these 20; all are
`lane=cli` (16 `office.actor.upsert`, 4 `office.surface.update`) and **zero** are `lane=rpc`.
The lane came up 15 s later (`L:5241 [05:48:46.377] subscribed (start) at baseline 88844923`)
and **the 5 writes were not re-attempted** — the next flush (`L:5245 [05:49:39.067] flush:
1 upserted`) is a fresh drop gesture.

That session's serve spawn was pathologically slow (`gate=25156ms spawn_to_booting=20571ms`,
`L:5243`; `S:1582 interpreter_ms=20421`), which is why its window was ~5.5 s wide instead of
the usual ~4.3 s.

**EG-4.0's mechanism already ships.** The `hold:` receipt exists **exactly once** in the log —
`L:16920`, in S15 — and it is BW-L6's hold doing precisely what EG-4.0 specifies: stage the
gesture during `laneAbsent`, flush it on the lane-up edge, deliver it on RPC. Combined with the
0 % share, **EG-4.0 closes as measurement**, its stage body unspent, and Plan EG §6.3's
adversarial worry (*"TC-3's window may never flatline if EG-4.0's pricing is wrong"*) resolves
in the favourable direction. Plan EG §3's own conditional — *"if the share is ~0, this stage
closes as measurement and TC-3 is unblocked for free"* — is the branch the receipts take.

### 2.3 `REVISION MISS` — EG-5.1's exposure

**12 lines, 3 sessions, 100 % `lane=rpc guarded=false`.**

| Session | when (local) | count | `predicted` → `server` |
| --- | --- | --- | --- |
| S2 | 11:29:39.505–.769 | 5 | 1 → 8, 19, 2, 7, 2 |
| S6 | 19:21:55.773–.856 | 3 | 1 → 10, 21, 9 |
| **S15 (post-BW)** | 08:28:54.766–55.029 | **4** | 1 → 11, 12, 30, 11 |

Rate: **12 misses / 51 RPC write attempts ≈ 24 %**, and **12 of 12 on the page-open batch** —
every one is a clustered burst inside one boot's first flush, none on a drag or drop gesture.

**Guarded refusals: 0.** Necessarily: nothing sends the token (§3.1). The receipt itself says
so — `guarded=false` on all 12.

**This contradicts Plan EG §3's framing of EG-5.1** (*"R0-b's REVISION-MISS rate decides the
rollout note, not the decision"*). The mechanism, not the rate, is the problem: the page-open
path predicts `1` — correct only for a CREATE
(`mission_office_layout_controller.dart:469-475`) — for actors the server holds at revisions 2
through 30. `_predictedRevision` (`:138-141`) returns `expectRevision + 1` when a token exists
and the create answer otherwise. Arming `expect_revision` on the remove/surface arms does not
touch these upserts, but **the page-open re-upsert storm is where 100 % of the field's revision
divergence lives**, and any extension of the guard to that path refuses **12 of 12**.
**Recommended adjustment:** EG-5.1's rollout note must state explicitly that the guard is
**not** armed on the page-open migration/re-upsert path, and that doing so is blocked on Plan
EG §5's unowned item 9 (the page-open write storm) — which these receipts promote from "largest
un-investigated behaviour" to "the sole source of measured revision divergence".

### 2.4 TC-0's surviving question — how often Lane A runs as a real child

Folded here per Plan EG §3. **S carries no office-lane, patch, or stream-frame receipts at
all.** Line-shape census of `agent.log` (1,755 lines): 73 × `snapshot_build reason=hydrate`,
5 × `harness serve boot timeline`, the remainder credential-pool / plugin-registry /
auxiliary-client noise. Zero occurrences of `office`, `patch_gap`, `folder`, `stream_frames`,
`office_patch`, `REVISION`, `write lane` — confirmed across `agent.log`, `agent.log.1` (5 MB),
`agent.log.2` (5 MB) and `errors.log` (1.9 MB). **The serve-child log cannot answer the
question**, and the launcher side records only `stream_spawn` / `stream_first_frame` /
`stream_teardown` in R (56 / 50 / 6 across the ring's 4-day span) without a lane label. Carried
forward as an instrumentation gap for **EG-2.1**, whose launcher half already specifies
"each stream attachment logs op + purpose at subscribe" — that is the line this question needs.

---

## 3. R0-c — the Class-A prove-dead greps at today's heads

### 3.1 Reap 1 — the three `--expect-revision` argv lowerings

**launcher `ec852fc95`, `lib/features/mission_control/data/mission_control_bridge.dart`.**
Plan cites `:4090-4094`, `:4105-4109`, `:4130-4134`. **No drift** — the three office lowerings
sit exactly there:

| Verb | argv lowering | line |
| --- | --- | --- |
| `office.actor.upsert` | `harness office actor-upsert` | **:4090-4094** |
| `office.actor.remove` | `harness office actor-remove` | **:4105-4109** |
| `office.surface.update` | `harness office set-folders` | **:4130-4134** |

**Prove-dead.** The only production sites that can put the `expect_revision` key into an intent
`args` map are `mission_board_write.dart:166` and `:192`. The three office submit sites do not:

| Submit site | `args` keys |
| --- | --- |
| `mission_office_layout_controller.dart:1251-1263` (`office.actor.upsert`) | `workspace_id`, `actor_json` |
| `mission_office_layout_controller.dart:1521-1532` (`office.surface.update`) | `workspace_id`, `folders` |
| `mission_office_layout_controller.dart:1645-1659` (`office.actor.remove`) | `workspace_id`, `actor_key` |

These are the only three (`grep -rn "office\.actor\.upsert'\|office\.actor\.remove'\|office\.surface\.update'" lib/`
returns exactly `:1253`, `:1523`, `:1650`, plus `office.resolve_conflict` at `:801` which takes
no revision on either lane). **CONFIRMED DEAD.** Corroborated in the field: 0 guarded writes in
12 `REVISION MISS` receipts (§2.3), and the code's own confession at
`mission_office_layout_controller.dart:1638-1644`.

**⚠ Precision correction the reap commit must carry.** There are **five** `--expect-revision`
argv lowerings in `mission_control_bridge.dart` at HEAD, not three:

| line | verb | status |
| --- | --- | --- |
| **:4009-4012** | `board.card.move` | **LIVE — DO NOT DELETE.** Fed by `mission_board_write.dart:166`; fenced by `mission_board_projection_test.dart:329,347` (`expect(argv, containsAllInOrder(['--expect-revision','2']))`) |
| **:4046-4049** | `board.card.edit` | **LIVE — DO NOT DELETE.** Fed by `mission_board_write.dart:192`, via `mission_board_card_panel.dart:233` / `mission_board_drawer.dart:112` (`expectRevision: card.revision`) |
| :4090-4094 / :4105-4109 / :4130-4134 | the three office verbs | dead — reap these |

A `grep`-driven reap that deletes by pattern rather than by verb breaks the board write lane.
Also part of the removal contract: the capability registry's `allowedArgs`
(`harness_capability_registry.dart:164, 184, 233, 248, 270`) and the argv contract fixture
(`test/features/mission_control/fixtures/hermes_cli_contract.json`, six
`"--expect-revision"` entries) both enumerate the flag; `harness_capability_argv_test.dart:706`
(`'expect_revision' => 2`) synthesises it generically across capability ids, so it will need
narrowing to the board verbs when the office `allowedArgs` entries go.

### 3.2 Reap 2 — `_fetch_usage_lane`'s `fetch_account_usage` fall-through

**hermes `d0a3b63c7d`, `hermes_cli/harness.py`. Plan cite `:3355-3357` is STALE → the
fall-through is at `:3702-3704` (drift +347).** The tuple cite `:164-169` is unchanged.

```
hermes_cli/harness.py:3702-3704
    from agent.account_usage import fetch_account_usage

    return fetch_account_usage(provider_id)
```

**Prove-dead — the chain is filter-only and therefore closed:**

1. **The tuple.** `_USAGE_LANE_PROVIDERS` (`:164-169`) = `("openai-codex", "anthropic",
   "openrouter", "nous")`. Exactly **two** references repo-wide: the definition and `:3810`.
   No other assembly path exists.
2. **The only producer of ids** is `_detect_usage_candidates` (`:3809-3814`), and it never
   ADDS — it only filters:
   ```
   providers = _USAGE_LANE_PROVIDERS
   if only_provider:
       norm = str(only_provider).strip().lower()
       providers = tuple(p for p in providers if p == norm)
   return [p for p in providers if _usage_lane_detected(p)]
   ```
   Therefore `candidates ⊆ _USAGE_LANE_PROVIDERS`.
3. **A fifth id cannot even reach the function.** `--provider <unknown>` yields an empty tuple
   at `:3813`; `_cmd_usage`'s `if not candidates: return payload` (`:3887-3888`) returns before
   any dispatch.
4. **The only production caller** is `_fetch_usage_lanes:3833`
   (`pool.submit(_fetch_usage_lane, p) for p in candidates`), itself called only from `:3890`
   with those candidates. `grep -rn "_fetch_usage_lane\b" --include=*.py .` returns the
   definition, `:3833`, two doc-comment references (`:3677`, `:3781`), and test monkeypatches
   only.
5. **All four ids return before the fall-through:** `nous` `:3684`, `openai-codex` `:3690`,
   `anthropic` `:3694`, `openrouter` `:3698`.

**CONFIRMED DEAD.** `:3702-3704` is unreachable from production, and — per RD §0.4 — it is the
one surviving route back into the upstream blanket-swallow (`agent/account_usage.py:884-902`)
that S1 routed around, so it is dead code that is also a loaded regression. The second witness
Plan EG §2 specifies (a monkeypatched `fetch_account_usage` recorder asserting ZERO calls) is
buildable: `tests/test_harness_usage.py:265-270` already isolates the "which path
`_fetch_usage_lane` takes" axis without patching the function.

### 3.3 Reap 3 — the two test-only fold copies

Both **CONFIRMED `@visibleForTesting` with zero production callers.**

| Copy | Definition | Annotation | Production callers in `lib/` |
| --- | --- | --- | --- |
| `missionOfficePatchFoldOnto` | `mission_office_subscribe_lane.dart:183-212` | `@visibleForTesting` at **:182** | **0.** Other `lib/` hits are doc-comment references only (`:31`, `:327`, `:774`). Production mounts `missionOfficePushFoldOnto` (`:214+`), wired at `:853` |
| `MissionReadModel.applyPatchFrame` | `mission_read_model.dart:840` | `@visibleForTesting` at **:839** | **0.** Other `lib/` hits are doc references only (`:242`, `:1013`). Production uses the `prepareFold` → off-isolate map → `commitFold` split |

Line drift from the plan: `subscribe_lane:183-212` **exact**; `read_model:840` **exact**.

**Tests the reap stage must re-point.**

`missionOfficePatchFoldOnto` — **1 file, 6 call sites:**
- `test/features/mission_control/office/mission_office_subscribe_lane_test.dart:425, 470, 501, 524, 554, 886` (its purpose is declared at `:22`)

`applyPatchFrame` — **7 test files + 1 shared support helper, ≈58 direct call sites:**
- `test/features/mission_control/mission_read_model_patch_test.dart` (21 sites)
- `test/features/mission_control/mission_read_model_office_patch_test.dart` (28 sites)
- `test/features/mission_control/mission_stream_contract_fixture_test.dart` (:337, :408, :510)
- `test/features/mission_control/mission_fold_log_receipt_test.dart` (:106, :155)
- `test/features/mission_control/mission_read_model_persona_carry_test.dart` (:149)
- `test/features/mission_control/office/mission_office_push_fold_intake_test.dart` (:634, :838)
- **`test/support/mission_read_model_parity.dart:83`** — a shared helper, so every consumer of
  the parity comparator calls it transitively. Its one consumer today is
  `test/features/mission_control/mission_read_model_parity_test.dart:3`.

**Honest-exit flag, per Plan EG §2 and §6.5.** The `applyPatchFrame` reap is the larger of the
two by an order of magnitude (≈58 direct sites across 7 files, plus the parity comparator whose
own docstring names it as a driver — `mission_read_model.dart:822`, *"the comparator and unit
tests drive this sync path"*). `test/support/mission_read_model_parity.dart:83` is the strongest
candidate for the named honest exit (keep, rename, document): a *parity comparator* needs a
synchronous same-isolate fold by construction, which is the one thing the production
`prepareFold` → off-isolate → `commitFold` chain deliberately does not offer. Recommend the
reap stage sequence `missionOfficePatchFoldOnto` first (1 file) and treat `applyPatchFrame` as
its own commit with the comparator seam decided on inspection.

### 3.4 Keep-list re-confirmed at HEAD

`office.actor.restore` — argv lowering present (`bridge.dart:4112-4121`), zero UI submit sites
(`grep` for `office.actor.restore'` in `lib/` returns the bridge only). Classification
unchanged: **keep** (operator recovery lane, RD Correction 4).
`office.resolve_conflict` — argv lowering at `bridge.dart:4137-4148`, submit site
`mission_office_layout_controller.dart:801`, **no RPC method** — the unfinished main path
EG-4.5 owns. Both confirm Plan EG §0's convergence (d).

---

## 4. R0-d — `patch_gap` / `fold:gap`

### 4.1 The grep the brief specifies returns a false zero

`grep -c "patch_gap" eternia_launcher_diag.log` → **0**. `grep -c "fold:gap"` → **0**.
Neither is evidence of zero gaps. The `patch_gap` reason string is minted at
`mission_read_model.dart:915` (`_recordDrop('patch_gap:base=$baseOffset,held=$_sequence')`) and
routed by `_recordDrop` (`:291-294`) to `onDrop`, which `mission_control_bridge.dart:769-776`
forwards **into the transport receipt ring as `MissionTransportReceiptKind.readModelDrop`** —
a separate JSONL file (`mission_transport_receipt_log.dart:15, 89`:
`<app-support>/diagnostics/mission_transport/receipts.jsonl`), **not** the diag log. The diag
log renders the same event with different wording — `REFUSED gap:` — and `fold:gap` would only
appear as a *resubscribe reason* (`mission_office_subscribe_lane.dart:680`
`_requestResubscribe('fold:${verdict.resyncReason}')`), which requires the office push lane
specifically to be the one that noticed.

**Register note for future stages:** anyone pricing gap urgency must read R, not L.

### 4.2 The count, bracketed

**R (`receipts.jsonl`, 972 lines, `2026-08-14T00:26Z` → `2026-08-17T12:30Z`): 51
`read_model_drop` receipts, of which `patch_gap` = 1.**

```
2026-08-16T21:40:06.163718Z  patch_gap:base=88714711,held=88717592
2026-08-16T21:40:06.163718Z  {"kind":"fold_resync","gen":0,"detail":"gap"}
```

Its diag-log twin, same millisecond:

```
L:1380 [17:40:06.163] [MissionFold] REFUSED gap: base=88714711 held=88717592 — 2 rows, 0 applied (office_actor x1, persona_instance x1); discontinuous base, re-hydrating
```

`REFUSED gap` in L: **1**. (For contrast: `REFUSED fenced` **14**, `REFUSED actor` **20**.)

**Totals: 1 gap in ~4 days. Post-BW (S15): 0 — and 0 fold lines at all in that session, so the
post-BW figure is a null, not a rate.**

Day brackets of the 51 drops: 08-14 ×1, 08-15 ×17, 08-16 ×24, 08-17 ×9. Composition:
`stale_delta` 17, `stale_patch` 15, `stale_heartbeat` 11, `stale_hydrate` 7, **`patch_gap` 1**;
and — the ones that matter for Phase 1 — `heartbeat_gap` **0**, `patch_without_base` **0**,
`patch_without_entries` **0**, `delta_without_core` **0**, `persona_instances_carried` **0**.

The single gap's context (`L:1364-1385`) is a burst of drop gestures at 17:40 with five
resubscribes inside 15 s; the gap fired between an in-flight `push:full_core` resubscribe armed
at `:05.852` and its completion at `:07.153`, and the very next frame folded cleanly
(`L:1381 [17:40:06.561] applied 1 of 1 rows at 88717592->88718622`).

### 4.3 What this prices

**EG-1.1 (RD-H2, the `or 0` baseline) and EG-2.2 (RD-L3, `onReconnected`) urgency: LOW on this
evidence.** Neither defect's observable fired in ~4 days of the operator's real use. Two honest
caveats: (i) `patch_without_base` and `patch_without_entries` — EG-1.2's and EG-1.4's
observables — are **also 0**, so this window does not exercise the loss holes at all rather
than showing them closed; (ii) EG-1.1's own defect class (an unreadable event log minting
baseline 0) has **no receipt in either channel** — a fabricated baseline is by construction
invisible, which is the stage's whole point. Urgency stays a correctness argument, not a
frequency one. Notably `patch_without_entries` being 0 means EG-1.2's *producer-side* swallows
have no field witness either; the stage's justification remains structural.

Also recorded, extending RD §1.6's note: **zero `serve_office_subscription_rebaselined`
receipts** in either channel, and **11 `subscribed (start)` / 14 `push:full_core` / 11
`fold:fenced`** subscribe lines against **14 `push:full_core` + 11 `fold:fenced`** resubscribe
requests — a 1:1 pairing, so no resubscribe went unanswered. The backoff ladder reached
`resubscribe #5` **three** times (`L:1395` and `L:4993` `push:full_core`, `L:5191`
`fold:fenced`) and the cumulative `resubscribes` counter peaked at **9** (`L:5192`,
`subscribed (fold:fenced) at baseline 88820807 — 8 items, folded 6, resubscribes 9`); the park
threshold (5 per 60 s) was touched three times but
the log carries **no park receipt** — recorded, not analysed (Plan EG §5 no-go boundary).

---

## 5. Register hygiene — the exact stale sentences RD §1.7 enumerates

Plan EG §4.8 directs that these be struck **in place, citing Plan EG**, when EG-0.2 lands.
**Not edited by this document.** Enumerated with file + line so the striking edit is mechanical:

| # | RD §1.7 clause | File | Line | The stale text |
| --- | --- | --- | --- | --- |
| 1 | gesture plan §10.3 item 6 | `docs/agent-runtime-harness/OFFICE_GESTURE_FOLD_PROMOTION_PLAN_2026-08-16.md` | **1120** | *"Also fixes the boot demote for free: one `office.surface.updated` currently sinks a 23-event startup batch."* |
| 2 | item 9's stale half | `docs/agent-runtime-harness/OFFICE_GESTURE_FOLD_PROMOTION_PLAN_2026-08-16.md` | **1150** | *"Its surface write demotes the boot batch every time."* (item 9's write-storm half, `:1149-1152`, **stands** — §2.3 strengthens it) |
| 3 | same claim, restated | `docs/agent-runtime-harness/SCOPED_INVALIDATION_PLAN_2026-08-16.md` | **27** | *"…the boot demote fix (one `office.surface.updated` sinking a 23-event startup batch buys a full core today…)"* |
| 4 | same claim, as a measured finding | `docs/agent-runtime-harness/SCOPED_INVALIDATION_PLAN_2026-08-16.md` | **71-73** | *"**The boot demote.** One `office.surface.updated` sinks a 23-event startup batch (RELAYED-§10.3 item 6; consistent with the coverage table — surface events are deliberately uncovered, `patch_coverage.py:199-207` READ)."* — the parenthetical is doubly stale: surface events are **covered** since WV-H3 (`patch_coverage.py:179, 254`) |
| 5 | all-four-verbs-ride-RPC parity | `docs/agent-runtime-harness/OFFICE_WRITE_VERBS_RPC_PLAN_2026-08-16.md` | **70-71** | *"**The write chokepoint.** All four verbs already flow through ONE launcher file, `mission_office_layout_controller.dart`"* — true of the *chokepoint*, but read as a transport-parity claim it is false: `resolve_conflict` has no RPC method (§3.4, RD Correction 4). Strike or qualify as "one launcher file, not one transport" |
| 6 | absence-means-delete cited as extant | `docs/agent-runtime-harness/UNIFIED_GESTURE_PREDICTION_PLAN_2026-08-16.md` | **166** | *"…and — after Stage UP-4 proves it — the absence-means-delete branch in `_flush`."* |
| 7 | same, in UP-4's body | `docs/agent-runtime-harness/UNIFIED_GESTURE_PREDICTION_PLAN_2026-08-16.md` | **243** | *"When the log is clean on real gestures, delete the absence-means-delete branch."* |
| 8 | the branch as a past incident | `docs/agent-runtime-harness/UNIFIED_GESTURE_PREDICTION_PLAN_2026-08-16.md` | **322** | U-10 row: *"The absence-means-delete branch archived four actors"* — **historical, keep**; RD's clause targets briefs citing it as *extant*, not the incident record |
| 9 | superseded WV-0 A-1 | `docs/agent-runtime-harness/OFFICE_WRITE_VERBS_RPC_PLAN_2026-08-16.md` | **290-295** | WV-0's A-1 verification task — **discharged by §1.5 above**; mark ANSWERED rather than open |

RD §1.7's fourth clause (`03-retirement-ledger.md` no longer exists) needs no edit: the file is
absent from `docs/agent-runtime-harness/` at HEAD, and `19-deferred-debt-ledger.md` is present.

---

## 6. Everything this stage could NOT answer

Stated so no later stage inherits a silence as a fact:

1. **Whether a folder-only batch promotes.** The promotion decision happens in
   `patch_coverage.py` and emits **no receipt on either side of the wire**. §1.3 proves the
   sink drops it *if* promoted and *also* proves the client declares the tokens, which makes
   promotion the expected outcome — but "expected" is not "recorded". Deciding it needs either
   a hermes-side coverage receipt (a natural addition to **EG-2.1**'s receipt work) or a live
   folder gesture after EG-1.3.
2. **How often Lane A runs as a real child** (TC-0's survivor) — §2.4; no lane label exists in
   any channel. Carried to EG-2.1.
3. **Any hermes-side view of the office lane.** S is office-blind across 12 MB of logs (§2.4).
   Every finding above about the office lane is launcher-sided by necessity, which is exactly
   the diagnosis-loss **EG-2.3**'s correlation id exists to end.
4. **Whether the 20 refused pre-BW writes lost operator state.** The receipts show 5 staged
   writes refused 20 times and never retried (§2.2). Whether that mattered depends on whether
   the surface write carried a real folder change — unknowable from the receipt, which logs no
   payload. A `maskedCount`-style content receipt (**EG-5.2**) is what would have answered it.
5. **A post-BW rate for anything.** S15 is one boot, one flush, 34 minutes, zero folds. Its
   0 % `laneAbsent` share is a clean single observation of the *mechanism* (the hold fired and
   delivered), not a frequency. EG-4.3's formal window still owns the rate.

---

*Written 2026-08-17 as EG-0.2's discharge. Read-only; the striking edits of §5 and the reap
commits of §3 belong to EG-0.3 and the register-hygiene edit respectively. Three findings
adjust Plan EG and are flagged in place: §1.6 (the stream-child reap's precondition is a
post-condition), §2.3 (EG-5.1's exposure is mechanistic, not rate-driven), §2.2 (EG-4.0 closes
as measurement — its design already ships as BW-L6's hold).*
