# In-turn tools — the full inventory

GENERATED by `scripts/emit_harness_tool_inventory.py` from the live tool registry. Do not edit by hand; run the emitter with `--write`.

`harness_core` = 15 member toolsets, **44 callable tools**, ~1177 model tool tokens (`tool_name_envelope_v1` heuristic, not a provider bill). This is what every Eternia persona's harness lane resolves unless its profile declares something else — see canon `05-chat-turn-lane.md` §4c.

`gated` means the tool carries a `check_fn` (desktop/service availability), so it may be absent on a given box. `mutating` means it crosses the mutation boundary (`tool_permissions.READ_ONLY_BLOCKS`).

| tool | toolset | mutating | gated | description |
|---|---|---|---|---|
| `agent_chat_dispatches` | `agent_chat` | — | — | List your in-flight and recent background dispatches (read-only). |
| `agent_chat_installs` | `agent_chat` | — | — | List the other installs (machines) paired with this one, or one install's agent roster (read-only, no mint). |
| `agent_chat_log_path` | `agent_chat` | — | — | Get the file path of a teammate thread's live transcript log to grep/tail the full history (read-only, no mint). |
| `agent_chat_open` | `agent_chat` | — | — | Review the recent message tail of your shared thread with a teammate (read-only, no mint). |
| `agent_chat_send` | `agent_chat` | — | — | Send a chat message to another Harness persona and return their reply, or dispatch it in the background with wait=false (agent-to-agent chat). |
| `agent_chat_threads` | `agent_chat` | — | — | List your agent-to-agent chat threads with reachable teammates (read-only, no mint). |
| `board_card_add` | `board` | — | — | Add a planning card to the Mission Board (advisory; never mutates a goal). |
| `board_cards` | `board` | — | — | List active Mission Board cards for the current workspace. |
| `browser_back` | `browser` | — | yes | Go back to the previous page in browser history. |
| `browser_cdp` | `browser-cdp` | — | yes | Send a raw Chrome DevTools Protocol (CDP) command. Escape hatch for browser operations not covered by browser_navigate, browser_click, browser_console, etc.... |
| `browser_click` | `browser` | — | yes | Click an element by its snapshot ref ID (e.g. @e5). Requires a prior browser_snapshot. |
| `browser_console` | `browser` | — | yes | Get the current page's console output and uncaught JS errors (log/warn/error/info); pass `expression` to evaluate JS in the page and return the result. Use t... |
| `browser_dialog` | `browser-cdp` | — | yes | Respond to a native JavaScript dialog (alert / confirm / prompt / beforeunload) that is currently blocking the page. **Workflow:** call ``browser_snapshot``... |
| `browser_get_images` | `browser` | — | yes | List all images on the current page with URLs and alt text (find images to analyze with vision_analyze). |
| `browser_navigate` | `browser` | — | yes | Navigate to a URL and load the page; must be called before other browser_* tools. Returns a compact snapshot with ref IDs. Disambiguator: for plain info or p... |
| `browser_press` | `browser` | — | yes | Press a keyboard key (Enter to submit, Tab to navigate, or a shortcut). |
| `browser_scroll` | `browser` | — | yes | Scroll the page in a direction to reveal content above or below the viewport. |
| `browser_snapshot` | `browser` | — | yes | Get a text accessibility-tree snapshot of the current page with ref IDs (@e1...) for browser_click/type. full=false compact (default), full=true complete. Ov... |
| `browser_type` | `browser` | — | yes | Type text into an input field by its snapshot ref ID (clears the field first). |
| `browser_vision` | `browser` | — | yes | Screenshot the current page to inspect it visually (CAPTCHAs, layout, visual checks). Native-vision models see it next turn; others get an auxiliary text ana... |
| `clarify` | `clarify` | — | yes | Ask the operator a question before proceeding. Multiple choice: pass up to 4 `choices` (rendered as pickable rows -- never enumerate options inside `question... |
| `close_terminal` | `terminal` | — | yes | Close the read-only GUI tab for a background process (desktop only): drops the view, does NOT kill the process (output keeps buffering; reopen from the statu... |
| `delegate_task` | `delegation` | — | yes | Spawn one or more subagents in isolated contexts. Description is rebuilt at every get_definitions() call to reflect the user's current delegation limits. |
| `execute_code` | `code_execution` | — | yes | Run a Python script that calls Hermes tools via `from hermes_tools import ...` (web_search, web_extract, read_file, write_file, search_files, patch, terminal... |
| `focus_pane` | `terminal` | — | yes | Reveal and focus a pane in the Hermes desktop app when the user asks to see it — e.g. "show me the terminal", "open the file browser", "show the diff". Panes... |
| `memory` | `memory` | — | yes | Save durable facts to persistent memory that survive across sessions. Memory is injected into every future turn, so keep entries compact and high-signal. HOW... |
| `open_preview` | `terminal` | — | yes | Open something in the preview pane beside the chat in the Hermes desktop app. Use this when the user asks to see a page, dev server, or file in the preview p... |
| `patch` | `file` | yes | yes | Targeted find-and-replace file edits with fuzzy matching; returns a unified diff and auto-runs syntax checks. mode='replace' (path/old_string/new_string) or... |
| `process` | `terminal` | — | — | Manage background processes from terminal(background=true). Actions: list, poll (status + new output), log, wait, kill, notify (deliver the result in a NEW t... |
| `react_to_message` | `terminal` | — | yes | React to a message with a single emoji, the way you'd tapback in iMessage. Reach for it when a reaction is what a person would do: something funny gets a 😂,... |
| `read_file` | `file` | — | yes | Read a text file with line numbers and pagination (offset/limit; large reads truncate on a line boundary with next_offset). Auto-extracts .ipynb/.docx/.xlsx.... |
| `read_terminal` | `terminal` | — | yes | Read what is currently shown in the Hermes desktop GUI's embedded terminal pane (desktop only). No args = visible screen + total_lines; pass start_line/count... |
| `search_files` | `file` | — | yes | Search file contents (target='content', regex, ripgrep-backed) or find files by name/glob (target='files', sorted by mtime). Disambiguator: use instead of sh... |
| `session_search` | `session_search` | — | yes | Search or scroll the local session DB of past Hermes conversations (FTS5, no LLM). Shapes: query -> discover sessions; session_id + around_message_id -> scro... |
| `skill_manage` | `skills` | — | — | Create / update / delete skills (your procedural memory). Actions: create, patch (preferred for fixes), edit (full rewrite), delete (pass absorbed_into), wri... |
| `skill_search` | `skills` | — | yes | Search installed skills + the Hermes Skills Hub by query without loading SKILL.md bodies (compact ids/descriptions). Disambiguator: skill_view loads an insta... |
| `skill_view` | `skills` | — | yes | Load a skill's full SKILL.md plus its linked files (references/templates/scripts). First call returns SKILL.md + a linked_files dict; call again with file_pa... |
| `skills_list` | `skills` | — | yes | List available skills (name + description). Disambiguator: skill_search(query) finds matches; skill_view(name) loads full content. |
| `terminal` | `terminal` | yes | yes | Run shell commands on a persistent Linux env (cwd + exported env persist between calls). Foreground returns instantly; background=true (pair notify_on_comple... |
| `todo` | `todo` | — | yes | Manage the current session's task list (3+ step work). No args reads it; pass `todos` to write (merge=false replaces, merge=true updates by id). Items {id, c... |
| `vision_analyze` | `vision` | — | yes | Load an image (URL, local path, or data URL) into the conversation so you can see it. Native-vision models read the pixels next turn; others get an auxiliary... |
| `web_extract` | `web` | — | yes | Extract clean markdown/text from web page or PDF URLs (no LLM summarization). Large pages return a head+tail window with a saved-file path to read the rest.... |
| `web_search` | `web` | — | yes | Search the web (up to 5 results: title, URL, description). Backend operators like site:, filetype:, intitle:, -term, and "exact phrase" may work. Disambiguat... |
| `write_file` | `file` | yes | yes | Write a file, completely replacing its contents (creates parent dirs; auto-syntax-checks and surfaces only NEW errors). Disambiguator: overwrites the whole f... |

## CLI-only verbs

Operate-table rows with no in-turn tool. Reaching for the terminal for anything NOT on this list is a navigation failure worth reporting.

- `hermes harness persona instance open-chat`
- `hermes harness mission-chat steer`
- `hermes harness mission-chat turn-resolve`
- `hermes harness mission-chat queue-skill`
- `hermes harness persona instance return-summary`
- `hermes harness persona instance steer`
- `hermes harness flow set`
