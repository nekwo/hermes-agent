# Stage 18 — complete CI failure review and isolated repairs

## Delivery status

Hermes main remains `34ad8ba33f2508ab10bb24a26f0377ddb62660cb` locally and on origin.
Launcher main is `241391dd66a55d4375fee81609c9195d8240c1b7` locally and on origin.
The approved Hermes maintenance window has not been used; the operator's gateway remains running.
No Launcher restart or runtime configuration edit occurred.

Recovery branch `codex/sync-regression-recovery-20260915` preserves candidate
`6139acf821` independently of the PR branch. Main still requires completed regression review.

## Complete CI result

Run `35012872230`, candidate `a3d33e95a904c7e981627a0d044a4ac6356a98b4`:

- Full Python: 4,785 files; 58,707 passed, 106 failed, 539 skipped; 3,772.7 seconds.
- Eight additional files reported collection, timeout, or nonzero teardown outcomes.
- Native Windows: 212 passed, 1 skipped, 1,684 deselected.
- JavaScript/TypeScript: all ten checks passed.
- Other reported PR checks passed, including 21 actual mutation claims.

Raw logs remain in ignored `qa-artifacts/python-ci-a3d-complete.log`; extracted per-file
failures are in `qa-artifacts/python-ci-a3d-failures/`. This is a failed full gate, not release proof.

## Repairs and focused evidence

All Python runs use `scripts/run_tests.sh` with private test environments.

- Skill inventory import cycle: 11 passed (`skill-import-order-fix.log`).
- Config defaults, unbounded persona posture and MCP env owners: 182 passed (`config-policy-owner-fix.log`).
- CLI aliases, profile credentials, surrogate sanitization and argument binding: 86 passed,
  one layout failure caused by the now-empty retired directory; layout rerun passed.
  The duplicate auth test shadowed a stronger test; preserved the stronger profile-precedence witness.
- Local-model quickstart deterministic hardware, read-only Tirith config and optional parent
  notification turns: 37 passed (`cli-policy-fixture-repairs.log`).
- Tirith security and five call sites: 52 passed (`tirith-owner-repairs.log`).
- Notification ownership and display: 12 selected tests passed (`notification-ownership-repairs.log`).
  Real fix: redact and strip terminal escapes before compacting a command title; raw credentials
  must not appear in process completion status. The model payload remains separate.
- Provider/vision/cache attribution: 137 passed and one browser fixture failure; the browser
  fixture was corrected to its actual loader and all 23 browser tests then passed.
- Environment reader witnesses, skip registry and browser: 68 passed (`env-reader-repairs.log`).
  Retired dashboard OAuth writer no longer exists; removed its stale platform skip rather than
  restoring a dead API. Current credential writers retain their separate tests.
- Frozen-home register: 4 passed (`frozen-home-ledger-repair.log`). Removed repaired upstream
  freezes, tracked relocated action-log owner, and documented intentional reassignment sentinels
  whose runtime accessors resolve the active home. No runtime path behavior was weakened.
- Code execution and mode/helper contracts: 66 passed, 25 platform skips on Windows
  (`code-kernel-contract-repairs.log`). Preserved the fork's stable concise description while
  documenting upstream persistent sessions; executable helper imports are served through
  the advertised full-description path. Linux execution coverage remains to rerun.
- Linux platform owners: 68 passed, one stale bytecode-directory layout failure
  (`linux-platform-owner-repairs.log`): web build 26, updater quarantine 32, Photon 6,
  line endings 3 and layout 1. Remove only the identified generated bytecode and rerun layout.

## Remaining

Continue the complete failure inventory, including tool-description contracts, path-identity
and documentation gates, service fixtures/timeouts, and remaining runtime ownership checks.
The separate hash-pinned upstream source-assertion register proposal is still awaiting a user
decision; existing closed registers have not been expanded. See `source-assertion-policy-decision.md`.
Rerun the repaired batch, then the full CI gate on the final candidate. Do not advance Hermes main
on the partial evidence above. Preserve the exact original/review tree-equivalence evidence and
history-preserving fast-forward delivery strategy from earlier stages.
