> Latest: [checkpoint 14](integration-stage14.md). Launcher updater protection is on main. Hermes remains an unlanded candidate with regression checks still in progress; earlier completeness statements are superseded. The live service still imports the primary checkout.

# Hermes upstream synchronization checkpoint — 2026-09-14

Status: inventory and recovery checkpoint; upstream integration is NOT validated
or landed. This report does not authorize replacing published main.

Navigation: [verification and exact commands](verification.md),
[installer handoff](installer-handoff.md),
[all original-to-review commit mappings](original-to-consolidated.csv),
[unresolved conflict ledger](conflict-ledger.csv).

Final checkpoint: original main and origin/main still match at `34ad8ba33f`;
all nine pre-existing worktree heads and porcelain statuses are unchanged
(`worktree-preservation.json`). The bounded review tip is `5104732788`, with
the exact original tracked tree. 394 baseline tests passed with one retry, and
the isolated real-model/tool probe exited 0. No integrated candidate exists yet.

## Pinned inputs and safety

| Input | SHA |
| --- | --- |
| Original local main and fetched origin/main | `34ad8ba33f2508ab10bb24a26f0377ddb62660cb` |
| NousResearch/hermes-agent advertised default main, fetched | `110baa095bc7135a0624557a9cc35df0f98ece0f` |
| Unique merge base | `126ff7071b6b755055879648f4e859b3187d0fac` |
| Previous upstream tracking tip | `562ee8ab76a703b7f524172cae0b1d52f9f94bd3` |
| Earlier fold archive | `4f06910b5ed393b88540d07c09d6d52d66a0cc97` |
| Original tracked tree | `5f5a34ad214d695f5c889a0798e220d175cc91ad` |

Origin is https://github.com/nekwo/hermes-agent; upstream is
https://github.com/NousResearch/hermes-agent. Both advertised HEAD as main via
`git ls-remote --symref`. Fetch changed remote-tracking refs only; some unrelated
upstream topic branches had force-updated remotely. Published fork main was not
rewritten. Recovery branches pushed to origin before any reconstruction:

- `codex/sync-backup-main-20260914`: original main.
- `codex/sync-backup-upstream-prefetch-20260914`: previous upstream tracking tip.
- `codex/sync-backup-upstream-20260914`: fetched upstream snapshot.
- The earlier archive branch and `pre-fold-main-20260731` tag were also verified
  on origin at the SHA above. They preserve the history underlying the old fold.

All registered worktrees in both repositories were inspected first. Preserved:
Hermes `X:/wt/local-llama-hermes` has seven untracked local-llama proof folders;
Launcher primary has modified/untracked `.claude/agent-memory/systems-engineer`
notes; `X:/wt/maps-voice` has seven modified and two untracked spatial-voice files.
Other registered worktrees were clean. Exact worktree records are in
`worktree-baseline.json`. No cleanup, stash, reset, service restart, runtime-config
change, or test in those worktrees was performed. Work began in a fresh worktree
from main, created from neutral cwd `X:/`.

The configured post-merge hook repairs runtime shared skills. Candidate/landing
merge commands must use a command-scoped disabled hook path to honor the operator's
no-runtime-change constraint; never change the clone's persistent hook setting.

## Inventory and attribution

`summary.json`, `commits.json`, `net-paths.csv`, and
`original-to-review-groups.csv` are generated by `inventory.py` against pinned
SHAs. All 1,172 reachable fork-only commits are included, not merely first-parent
history: 1,134 non-merges and 38 merges. Current author spellings are 1,138 nekwo
and 34 Tony Arena, both with the same email; that does not authorize erasing either
spelling. Six distinct coauthor trailers are retained verbatim in the inventory.
The archived pre-fold range contains 679 fork-only commits (675 nekwo, 4 Tony
Arena). Upstream ancestry and contributors remain intact in the original DAG.

`git cherry <pinned-upstream> <pinned-fork>` returned 1,134 `+` entries and zero
`-` entries (`patch-equivalence.txt`, exit 0). This means no exact non-merge patch
equivalence was found; it does NOT establish that upstream lacks semantically
equivalent fixes, nor evaluate merge resolutions. Never drop a patch based on its
name or author. The old fold explicitly included a prior upstream synchronization
inside `101d95eeb6`; replaying it as wholly original work would misrepresent it.

The original net delta is 1,541 paths, +544,215 / -2,519 lines. Of these, 1,208
are added files absent from current upstream; 313 paths exist on current upstream.
The downstream `agent_runtime/`, Harness command parts and `agent/charsheet/`
have no current upstream files. Preserve them intact, then review every import,
callback and contract seam. File isolation is not proof of API compatibility.

## Proposed review order (not an executable squash recipe)

These 15 themes provide navigation. They are NOT a claim that 1,172 commits fit
15 independently valid commits. The CSV's path-based labels are provisional,
many-to-many, and flag 45 net paths as `00-needs-manual-split`; those require
manual assignment. Shared-file commits must be split by intent, not assigned
wholesale based on filenames. Tests and design evidence should travel with the
behavior they prove when producing a final series.

| Order | Theme | Dependency / split rule |
| --- | --- | --- |
| 1 | Build, hermetic testing and Windows platform support | Establish isolation first; split runner, installer and CI policies |
| 2 | Runtime roots, storage, locks, migrations and events | Depends on 1; keep persisted-format migrations separate |
| 3 | Auth, configuration, provider selection and readiness | Depends on 2; preserve Codex pool versus auth-store authority |
| 4 | Persona and instance lifecycle | Depends on 2–3; identity and profile-binding invariants |
| 5 | Skills, tool admission and MCP | Depends on 2–4; separate tool schemas from admission policy |
| 6 | Serve ownership, authentication, pairing and transport | Depends on 2–5; transport-proven principal and root |
| 7 | Snapshot, stream, cache and observability | Depends on 4–6; ordering, revisions, replay and timing receipts |
| 8 | Office, board and graph state | Depends on 2,4,6–7; separate independently persisted families |
| 9 | Realm synchronization and artifact families | Depends on 5–8; three-way baselines and conflict decisions |
| 10 | Operator chat, admission, turns, dispatch and continuity | Depends on 3–7; cancellation and active-turn exclusion |
| 11 | Upstream agent/tool/gateway integration seams | Co-deliver with 3,5,6,10; cannot cleanly isolate all shared-file hunks |
| 12 | Character assets and generation | Depends on provider and runtime lifecycle; separate atlas and revisions |
| 13 | Mobile profiles/transport | Depends on 3,6–7; independent consumer contract |
| 14 | Managed Local llama runtime/provider | Depends on 3,6,10–11; preserve whole-turn ownership and local routing |
| 15 | Installer design and historical evidence | Installer stays proposed; move other docs beside owning behavior |

There are dependency cycles across upstream seams and downstream callers. A
file-partitioned 15-commit snapshot would be tree-equivalent at its tip but could
be broken at every intermediate commit. It is not an acceptable integration
series. Preserve history for delivery. A small Local llama review consolidation
can demonstrate a useful coherent series without pretending to squash the entire
fork. Original-to-consolidated mapping for that experiment is separate.

## Safe update path and exact fast-forward distinction

1. Start integration candidate at the pinned original main, NOT the review branch.
2. Merge the pinned upstream tip with both parents preserved. Resolve each shared
   path deliberately; keep new downstream files. Record each conflict decision and
   any semantic equivalence with evidence. Never blanket-select ours/theirs.
3. Test the finished candidate through `scripts/run_tests.sh`. Reconcile the old
   `scripts/upstream_sync_gate.py` recipe first: it still calls bare pytest, which
   contradicts current AGENTS.md and the operator's explicit wrapper requirement.
   Do not execute that unsafe recipe unchanged or silently claim its PASS.
4. Run isolated real-model proof and companion contract checks. No live service
   restart, primary configuration writes, fixture churn to hide regressions, or
   shared test-environment dependency upgrades.
5. Only after green evidence, ensure original main remains an ancestor. Fetch
   origin's main, re-check all worktrees and fast-forward the clean primary to the
   candidate, then push main. Bypass only the runtime-writing hook for this command.
6. If main moves, merge-preserving rebase or rebuild the UNPUBLISHED candidate on
   the new origin/main, rerun affected proof and publish a new recovery ref.

A merge commit on the candidate is compatible with **fast-forward landing**:
old main -> candidate merge is a descendant relationship. No ff-only rule change
is required. If ff-only is intended to prohibit merge commits anywhere in history,
that stricter interpretation conflicts with preserving both existing histories;
request the specific exception before landing. A reconstructed review branch
does not descend from published main and cannot replace it with a normal ff push.
No main force-push, `ours` strategy merge, or overwrite-by-snapshot is proposed.

## Merge preview

System Git 2.31.1 lacks `merge-tree --write-tree` (exit 128); bundled Git
2.53.0 ran the non-worktree-writing preview (exit 1). Raw output is
`merge-preview.txt`: 187 conflict notices, including modify/delete and add/add.
The output tree `8f51a1c3934b18f8fd12090fd7f4ab028e44e74e` contains unresolved
conflict material and is NOT a runnable or integrated tree. No conflict has been
declared resolved by this preview. Critical conflicts include auth.py,
runtime_provider.py, config.py, agent_init.py, auxiliary_client.py,
conversation_compression.py, conversation_loop.py, run_agent.py, tools/registry.py,
gateway/run.py, pyproject.toml, uv.lock and the test runner.

Keep the snapshot backups indefinitely. A review branch is not a replacement for
those originals, since historical tests and documents resolve earlier commit IDs.
